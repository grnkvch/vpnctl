# vpnctl v2 host change journal

This journal records development-host mutations made while implementing and validating vpnctl v2. Repository files and ordinary build caches under `/tmp` are excluded. Every entry names exact targets, conflict scope, verification, and rollback.

## Operating rules

1. Never modify, start, stop, or delete a host VM, service, package, network, or file that is not explicitly owned by the `vpnctl-v2` lab.
2. Lab VM names are reserved to `vpnctl-v2-gateway` and `vpnctl-v2-node`. The pre-existing `realty-front-docker-vm` is foreign and must remain untouched.
3. Before package-manager mutation, capture installed versions and disable automatic updates and cleanup (`HOMEBREW_NO_AUTO_UPDATE=1`, `HOMEBREW_NO_INSTALL_CLEANUP=1`).
4. Prefer project-local or `/tmp` tools. A host package install requires a recorded removal command and a compatibility note for upgraded shared dependencies.
5. Before every destructive rollback, resolve and verify the exact target. Never use broad Lima or Homebrew cleanup commands.
6. Generated VM evidence lives under ignored `artifacts/v2lab/`; accepted conclusions are copied into versioned ADRs. Secrets are never recorded here.

## 2026-09-01 — Lima lab bootstrap

### Failed shared-network gateway fixture

- Requested mutation: create `vpnctl-v2-gateway` from `test/v2lab/lima.yaml`.
- Observed result: Lima created a stopped instance directory, then refused to boot because `socket_vmnet` was absent.
- Cleanup performed: `limactl delete vpnctl-v2-gateway` after `limactl list --json` confirmed the exact instance was stopped.
- Verification: the named instance disappeared from `limactl list`; no `socket_vmnet` or sudoers changes were made.
- Conflict scope: none outside the deleted lab instance.
- Rollback: already complete.

### Rootless-network gateway fixture attempt

- Requested mutation: recreate `vpnctl-v2-gateway` with 1 vCPU, 512 MiB RAM, 10 GiB disk, x86_64 QEMU, and `lima:user-v2` networking.
- Observed result: a stopped exact-name instance was created; boot stopped before guest start because Lima's x86_64 guest agent was absent.
- Conflict scope: only `~/.lima/vpnctl-v2-gateway`; no foreign instance was invoked.
- Rollback: `limactl stop vpnctl-v2-gateway` if running, verify stopped state with `limactl list --json`, then `limactl delete vpnctl-v2-gateway`.

### Homebrew guest-agent installation incident

- Requested command: `brew install lima-additional-guestagents` to support an amd64 guest on an arm64 host.
- Intended changes: install `lima-additional-guestagents` and its new `json-c` dependency.
- Actual additional changes caused by Homebrew defaults:
  - auto-updated Homebrew taps;
  - upgraded Lima `2.1.3 → 2.2.0` and QEMU `9.2.2 → 11.1.1`;
  - upgraded shared dependencies including `libunistring`, `gettext`, `capstone`, `dtc`, `glib`, `ca-certificates`, `libtasn1`, `nettle`, `p11-kit`, `gnutls`, `jpeg-turbo`, `libpng`, `libslirp`, `openssl@3`, `libssh`, `libusb`, `ncurses`, `pixman`, and `snappy`;
  - Homebrew cleanup removed old formula versions and caches, auto-removed `python@3.13`, and removed a cached Clash Mi DMG (not the installed application).
- Verification: `limactl version` reports `2.2.0`; `lima-additional-guestagents 2.2.0` and QEMU `11.1.1` installed successfully.
- Conflict scope: upgraded Lima/QEMU may affect the next start of the pre-existing foreign `realty-front-docker-vm`; that VM has not been started, stopped, deleted, or edited by this work.
- Safe partial rollback: after deleting only the two lab VMs, run `HOMEBREW_NO_AUTO_UPDATE=1 HOMEBREW_NO_INSTALL_CLEANUP=1 brew uninstall lima-additional-guestagents` if x86_64 lab support is no longer needed.
- Exact rollback limitation: Homebrew cleanup removed prior Lima/QEMU/dependency cellars, so an exact automatic rollback is not currently guaranteed. Restoring those versions would require explicit retrieval of the old signed bottles/formula revisions and a separately reviewed plan. No such rollback will be attempted implicitly.
- Prevention: all subsequent Homebrew commands must set both opt-out environment variables and record a preflight version snapshot before execution.

### Active lab creation

- Requested mutation: start `vpnctl-v2-gateway` and `vpnctl-v2-node`, downloading only the pinned Ubuntu 24.04 amd64 image digest and writing baseline reports under ignored `artifacts/v2lab/`.
- Conflict scope: the two exact lab instances, Lima's image cache, and the isolated `192.168.104.0/24` user-v2 network. No host listener or privileged socket-vmnet network is requested.
- Full lab rollback: run `./scripts/v2lab.sh down`, verify both exact names are stopped, then run `./scripts/v2lab.sh destroy`. The pinned downloaded image may remain in Lima's shared cache; cache removal is intentionally not automated because it can affect foreign instances.
- First result: both fixtures reached Lima `READY`; provisioning created the guest-local 1 GiB swap files and installed the lab packages. Helper installation then stopped without changing host state because clean Ubuntu lacked `/usr/local/libexec`.
- Corrective action: change the guest-local helper installation to `install -D`, which creates only the missing parent directory and the two owned files `/usr/local/libexec/vpnctl-v2-lab-{report,fault}`. Re-running `up` reuses the two exact fixtures.
- Corrective rollback: deleting the lab fixtures removes these guest-local files; while retaining the fixtures, remove only the two exact paths and remove `/usr/local/libexec` only if it is empty.
- Second result: helper installation succeeded on both fixtures. Baseline serialization stopped because two memory metrics contained a literal `\\n`; the zero-byte partial `artifacts/v2lab/20260901T180558Z/gateway.json` is ignored and has no host effect. The same diagnostic exposed a positional `vmstat` parser that did not account for Ubuntu's additional `gu` CPU column.
- Corrective action: emit real newlines from the two `awk` expressions and select `vmstat` fields by the `us`, `sy`, `id`, and `wa` header names. Reinstalling the exact helper paths overwrites only lab-owned files.
- Baseline result: both reports completed at `artifacts/v2lab/20260901T180732Z/summary.json`; each guest reports Ubuntu 24.04 amd64, 1 vCPU, 453 MiB usable RAM inside the 512 MiB VM limit, 1 GiB swap, a 10 GB provisioned disk (8.65 GiB filesystem), and successful peer connectivity. An escalated final Lima metadata snapshot is still pending.
- Status: fixtures running; baseline complete.

### Planned lab network-fault verification

- Requested mutation: on `vpnctl-v2-node` only, apply a 250 ms `netem` delay toward the gateway, replace it with an nftables partition limited to the gateway peer IP, then clear both controls.
- Conflict scope: guest-local `eth0` root qdisc and the dedicated guest-local `inet vpnctl_v2_lab_fault` table. Host interfaces, host firewall, and the gateway guest are not modified.
- Rollback before every transition and after the test: `./scripts/v2lab.sh fault node clear`. The helper deletes only a detected `netem` root qdisc and the exact dedicated nftables table; deleting the node fixture is the fallback full rollback.
- Verification: capture a report after latency, partition, and clear; finish only after `tc qdisc` and the dedicated nftables table are absent and peer connectivity is restored.
- Result: `artifacts/v2lab/20260901-latency-250ms/summary.json` measured 250.974–253.781 ms and 0% loss; `artifacts/v2lab/20260901-partition/summary.json` measured 100% peer loss in both directions; `artifacts/v2lab/20260901-cleared/summary.json` measured restored 0% loss and 1.152–2.451 ms latency.
- Conflict hardening: netem now uses the dedicated `1abc:` qdisc handle on only the peer-route interface, and clear refuses to remove another qdisc. Lima orchestration validates VM type, architecture, exact resource limits, image digest, and network before reusing, stopping, or deleting an exact-name instance; deletion also refuses running instances.
- Final verification: the dedicated nftables table was absent, the ordinary `fq_codel` qdisc was restored, and a second dedicated-handle cycle showed `qdisc netem 1abc:` during the fault and restored 0% loss afterward at `artifacts/v2lab/20260901-final-cleared/summary.json`.
- Final Lima metadata: both lab fixtures are `Running`, `qemu`, `x86_64`, 1 vCPU, 536870912 bytes configured RAM, and 10737418240 bytes configured disk. The foreign `realty-front-docker-vm` was observed as `Running` but was not started, stopped, edited, or otherwise targeted by any lab command.
- Current rollback: `./scripts/v2lab.sh down` stops only contract-matching lab fixtures. After their stopped state is verified, `./scripts/v2lab.sh destroy` deletes only those fixtures. The fixtures remain running for the immediately following transport spikes.
- Status: complete; no network fault remains active.

## 2026-09-01 — Restricted transport spike preparation

### Planned Lima forwarding isolation

- Requested mutation: stop only `vpnctl-v2-gateway` and `vpnctl-v2-node`, add ignore rules for guest ports `1053`, `8443`, `17890`, `18080`, and `19090` to both exact instance configurations as applicable, then restart only those fixtures.
- Reason: prevent Lima's dynamic forwarder from creating development-host listeners when the guest spike starts its services.
- Conflict scope: only the two contract-matching lab instance configurations and their temporary downtime. The foreign `realty-front-docker-vm` is not a command target.
- Preflight: verify exact names, QEMU/amd64, 1 vCPU, 512 MiB, 10 GiB, pinned image digest, rootless network, and current running state before stopping or editing.
- Rollback: stop the two exact lab fixtures, remove only the five matching `portForwards` entries with a separately reviewed `limactl edit`, and restart. Keeping the ignore rules is safe and preferred because it prevents host exposure; deleting/recreating the fixtures from the versioned template produces the same isolated state.
- First stop result: no VM changed state because Lima 2.2 rejected a multi-name `stop` invocation before acting. The lab orchestrator was corrected to invoke `stop`/`delete` once per already contract-validated exact instance.
- Retry result: both exact fixtures stopped cleanly. Five `proto:any` ignore entries were added to each stopped configuration and a read-only snapshot confirmed the entries before restart.
- Boot observation: the already-created fixtures embed the original idempotent-but-networked provision script, so this restart repeats `apt update/install` and adds several minutes of amd64-emulated boot time. The versioned template now writes `/var/lib/vpnctl-v2-lab.provisioned` after success and skips package networking on subsequent boots of newly created fixtures. Existing fixtures are not edited out-of-band; they retain the old hook until safely recreated from the template.
- Restart result: both fixtures returned to `READY` and produced `artifacts/v2lab/20260901-port-isolation/summary.json`. No spike listeners exist yet; the ignore rules will be verified again after service start by checking both Lima metadata and host listeners.
- Status: complete; both fixtures running with forwarding isolation.

### Planned Mihomo/ShadowTLS candidate installation

- Requested mutation: download the official pinned Mihomo `v1.19.30` amd64 gzip into ignored `artifacts/v2lab/cache/`, verify SHA-256 `cf06ce2c7d1421bdbda14ee4a5b6046672dc35ebf8eecd8e77504ec3c0ed9a84`, and install the binary, generated configs, three systemd units, and three state directories only inside the two lab fixtures.
- Guest ownership: all paths use `vpnctl-v2-spike` names and `/etc/vpnctl-v2-spike/restricted/.owner`; existing paths without the exact marker and occupied ports are hard conflicts. Units are started but not enabled.
- Listener scope: gateway `8443/TCP` plus `127.0.0.1:18080`; node `127.0.0.1:{1053,17890,19090}`. Lima ignore rules prevent host forwarding. No `8443/UDP` listener is configured.
- External dependency: `www.microsoft.com:443`, already observed from the gateway with TLS 1.3 and valid certificate verification, is pinned for the spike. No automatic fallback is used.
- Logging: `info` is a temporary explicit opt-in inside disposable fixtures for test evidence. Credentials and generated client profiles remain mode `0600` under ignored artifacts and are not recorded in this journal.
- Rollback: `./scripts/v2restricted-spike.sh stop` stops only the exact units. `uninstall` first verifies the marker, then stops/cleans those units and removes only their exact unit, config, binary, and systemd-managed state paths. Deleting the two lab fixtures remains the full fallback. Host cache/evidence removal is manual and must target the exact ignored directory.
- Result: the official 18,868,732-byte archive was downloaded and checksum-verified; both rendered configs passed pinned `mihomo -t`; owner-marked files and three non-enabled units were installed and reached active state.
- Host isolation verification: with all guest services running, `lsof` found no development-host TCP listener on `1053`, `8443`, `17890`, `18080`, or `19090`.
- Status: installed and running inside the two fixtures; no host listener created.

### Planned restricted E2E and reconnect mutations

- Requested runtime mutations: temporarily select the deliberately wrong ShadowTLS host in the node's in-memory `RESTRICTED` group, require strict failure, restore the pinned selection, then separately stop and restart only `vpnctl-v2-spike-restricted-gateway.service` to verify node reconnect.
- Persistence/conflict scope: no generated config or desired state changes; the selection is controller-memory-only. A shell trap restores `RESTRICTED-VALID` on every exit. The reconnect trap restarts the exact gateway spike unit on every exit.
- Verification: selected TCP reaches only the gateway-loopback token, selected DNS returns through the proxy-bound DoH upstream, wrong-host strict mode fails, correct host recovers, gateway outage fails, restart recovers without node restart, and resource/socket evidence is captured under ignored artifacts.
- Rollback: restore `RESTRICTED-VALID`; start the exact gateway unit; if either cannot be confirmed, stop all three spike units with `./scripts/v2restricted-spike.sh stop` while preserving evidence.
- Automated E2E result: passed at `artifacts/v2lab/restricted-spike/evidence-final/`. The gateway-loopback token was unreachable direct and reachable through restricted; the proxy-bound DoH request returned A records; temporary `www.apple.com` selection failed certificate validation; pinned `www.microsoft.com` recovered; `8443/UDP` was absent.
- Reconnect result: passed at `artifacts/v2lab/restricted-spike/reconnect-final/`. The outage probe failed while the exact gateway unit was stopped, recovery succeeded on the first bounded attempt, gateway Mihomo changed PID `2200 → 2375`, and node Mihomo remained PID `2178`.
- Resource snapshot: gateway/node Mihomo RSS was approximately 38/46 MiB; total guest RSS approximately 307/304 MiB. Both guests retained 1 GiB swap and healthy peer connectivity.
- Cleanup verification: the in-memory selection is `RESTRICTED-VALID`; the gateway unit is running; the temporary secret-bearing `/tmp/clash-mi-lab.yaml` was removed. No fault or wrong-host selection remains active.
- Logging opt-in closure: after evidence capture, `./scripts/v2restricted-spike.sh stop` stopped all three exact spike units; `systemctl show` confirmed `ActiveState=inactive` for gateway, node, and echo. Generated configs and evidence remain for an explicit rerun; no restricted/test listener or continuing Mihomo test log remains.
- Current host state: both isolated lab VMs remain running for the next implementation step, while all restricted spike units are inactive. `./scripts/v2lab.sh down` remains the exact VM-level rollback.
- Status: automated mutations complete and left inactive. Actual Clash Mi compatibility remains a manual product gate, so OpenSpec task 2.2 is not marked complete.

## 2026-09-01 — Restricted UDP-over-TCP spike

### Planned UoT installation and fault verification

- Accepted gate change: the prior automated gateway/Linux-node evidence completes task 2.2; actual Clash Mi execution is deferred to the deployed-service v2.0 release gate and remains mandatory before production approval.
- Requested guest mutation: replace only the owner-marked restricted node/client candidate configs with pinned Mihomo UoT v2 enabled, add the exact lab-owned executables `/usr/local/libexec/vpnctl-v2-spike/{udp-echo,udp-probe}`, add `vpnctl-v2-spike-udp-echo.service`, and restart only the four spike units.
- Listener scope: the added echo service uses `18080/UDP` inside the gateway fixture. The existing Lima `proto:any` ignore rule for guest port `18080` prevents development-host forwarding. Restricted ingress remains only `8443/TCP`; no `8443/UDP` socket is configured.
- Positive verification: a SOCKS5 UDP-associate probe targets the gateway echo through Mihomo with `udp-over-tcp: true` and version 2. A temporary exact-name `inet vpnctl_v2_spike_uot_capture` table on each lab guest counts protected TCP and any node-to-gateway native UDP.
- Failure verification: select the explicitly UoT-disabled but TCP-capable negative-control outbound, require selected TCP to keep working, require the UDP probe to fail, and require both native-UDP counters to remain zero.
- Conflict scope: only the two exact contract-matching lab fixtures and the ignored evidence directory. The foreign `realty-front-docker-vm`, host firewall, host routes, and unrelated guest nftables tables are not targets.
- Automatic rollback: shell traps restore `RESTRICTED-VALID` and delete only `inet vpnctl_v2_spike_uot_capture` on either exit path. `./scripts/v2restricted-spike.sh stop` stops only the spike units and clears the same exact table. `uninstall` additionally verifies the owner marker before removing the exact unit/config/executable/state paths.
- Full rollback: stop and destroy only `vpnctl-v2-gateway` and `vpnctl-v2-node` through `scripts/v2lab.sh`; the ignored cache/evidence remains until separately reviewed for exact-path removal.
- First installation result: both pinned Mihomo configs validated and all four exact spike units reached active state. The first positive UDP probe timed out because its test destination was not in the node's selected rules and the journal showed `match Match ... using DIRECT`; the temporary gateway counter/drop guard prevented that test-only direct datagram from reaching the echo service.
- First-run rollback verification: the failure trap restored `RESTRICTED-VALID`; `inet vpnctl_v2_spike_uot_capture` was absent from both guests; all four spike services remained active for correction. Mihomo's local API reported `udp=true,uot=true` for `RESTRICTED-VALID` and `udp=false,uot=false` for the negative control.
- Corrective action: add the rendered gateway `/32` only to this spike's selected rules so the UDP test target exercises the intended restricted policy, then owner-checked reinstall and repeat the complete capture. No host or foreign-VM change is required.
- Second-run result: selected classification and UoT were confirmed on both sides, but gateway Mihomo could not send the decoded UDP datagram to its own non-loopback address and reported `netlinkrib: address family not supported by protocol`; no native UDP escaped and both capture tables were removed by the failure trap.
- Second corrective action: target the gateway loopback echo at `127.0.0.1:18080`, matching the established TCP probe topology. Add a separate node output counter for UDP to that exact loopback target so a direct fallback cannot be hidden by the topology; retain the outer gateway/native-UDP counters and closed `8443/UDP` assertion.
- Third-run result: UoT again reached gateway and was decoded with the correct `127.0.0.1:18080` target, but gateway Mihomo reported the same `netlinkrib: address family not supported by protocol` before sending to echo. Failure traps again removed both capture tables and restored the valid selector.
- Root cause: the restricted Mihomo systemd units allowed only `AF_INET`, `AF_INET6`, and `AF_UNIX`; Mihomo's UDP direct outbound performs an `AF_NETLINK` route lookup. TCP did not expose this sandbox omission.
- Corrective action: add `AF_NETLINK` only to the two restricted Mihomo units' `RestrictAddressFamilies`. Keep the echo unit unchanged and reassert that no `8443/UDP` socket exists.
- AF_NETLINK rerun result: positive UoT passed and the capture recorded protected TCP with zero native UDP. The UoT-disabled negative control exposed a Mihomo safety hazard: selecting an outbound with `udp=false,uot=false` caused one matched UDP datagram to be attempted through `DIRECT`; the dedicated loopback counter caught it and failed the run. No datagram reached gateway and both capture tables were removed by the trap.
- Corrective action: add a separate `RESTRICTED-UDP` readiness group. It selects the restricted path only after UoT validation; the broken-UoT scenario explicitly selects `REJECT-DROP` before probing while TCP remains on the working restricted outbound. Save local API evidence for both `uot=false` and the active reject guard.
- Architectural consequence: vpnctl must never rely on Mihomo's `udp=false` capability flag alone for fail-closed behavior. The production renderer/readiness transaction needs an explicit UDP reject target plus the independent kernel leak guard already required by the design.
- Explicit-guard result: passed. Positive capture recorded 13 protected TCP packets and zero native/direct UDP; the selected UDP echo returned `vpnctl-v2-uot-ok`. With runtime `udp=false,uot=false` and `RESTRICTED-UDP` set to `REJECT-DROP`, TCP remained healthy with 17 protected packets, the UDP probe timed out, and all node-gateway, node-loopback, and gateway-input leak counters remained zero.
- Reconnect result: restarting only `vpnctl-v2-spike-restricted-gateway.service` caused the outage probe to fail, then both TCP and UoT recovered on their first bounded attempts without restarting node Mihomo.
- Cleanup verification: runtime selectors were restored to `RESTRICTED-VALID` and `RESTRICTED-UDP → RESTRICTED`; the exact capture table was absent on both guests; `8443/UDP` remained closed.

### Correct wildcard forwarding isolation

- Unexpected host observation: while the spike units were active, Lima hostagent exposed `127.0.0.1:8443/TCP` and `127.0.0.1:18080/UDP` on the development host even though per-port ignore entries existed. Stopping the four exact spike units removed both listeners; all units are currently inactive.
- Root cause: the existing rendered ignore rules defaulted `guestIP` to `127.0.0.1`, while gateway spike listeners bind wildcard addresses. The running hostagent therefore did not match those rules.
- Official Lima rule selected: each exact ignored port uses `guestIP: 0.0.0.0`, explicit `guestIPMustBeZero: false`, `proto: any`, and `ignore: true`, which matches listeners bound on any guest interface under Lima 2.x semantics.
- Planned host mutation: with both exact lab VMs contract-validated and spike units inactive, stop only `vpnctl-v2-gateway` and `vpnctl-v2-node`, update only their five existing port-forward entries, restart the two lab VMs, rerun the spike, and confirm host `lsof` has no matching listeners while guest services are active.
- Rollback: stop those same two exact VMs, restore only the ten edited `guestIP`/`guestIPMustBeZero` values, and restart. The old values are known to expose local host listeners and therefore are retained only as an audit rollback, not a recommended state. Deleting/recreating the two lab VMs from the corrected versioned template is the clean full rollback.
- Applied correction: both exact lab VMs were contract-validated, stopped, and edited only at the five existing port-forward entries. Read-only metadata confirmed `guestIP=0.0.0.0`, `guestIPMustBeZero=false`, `proto=any`, and `ignore=true` on every entry before restart. The foreign VM was not a command target.
- Restart result: both lab VMs returned to `READY` and produced `artifacts/v2lab/20260901-uot-forwarding-fixed/summary.json`. The old embedded provision hook repeated inside each guest; no Homebrew or other host package mutation occurred.
- Isolation verification: after all four spike units were active and no `limactl shell` command remained, host `lsof` found no TCP or UDP listener on `1053`, `8443`, `17890`, `18080`, or `19090`.
- Final isolated E2E: `artifacts/v2lab/restricted-spike/uot-final-isolated/summary.json` passed TCP, DNS, strict-host, positive UoT, explicit broken-UoT reject, zero-leak counters, and closed `8443/UDP`. `uot-reconnect-final-isolated/summary.json` recorded TCP and UoT recovery on the first attempt.
- Logging/runtime closure: `./scripts/v2restricted-spike.sh stop` stopped the four exact units and removed the exact capture table. Both lab VMs remain running for the next spike, with spike services inactive and no development-host spike listeners.
- Status: task 2.3 complete; forwarding isolation corrected and verified.

## 2026-09-01 — Restricted UDP-over-TCP workload benchmark

### Planned benchmark mutations

- Requested guest-only mutation: owner-checked reinstall of the restricted spike adds two test executables (`udp-benchmark` and `http-benchmark`) plus a synthetic credential-free Telegram Bot API-shaped response. Only the existing four spike units and their already owned paths are restarted.
- Runtime benchmark mutation: select `RESTRICTED-VALID` and `RESTRICTED-UDP → RESTRICTED`, create only the exact temporary capture tables `inet vpnctl_v2_spike_uot_capture`, then apply a 250 ms node-to-gateway partition using the lab-owned `inet vpnctl_v2_lab_fault` table while one bounded UoT stream is active.
- Workloads: 50 sequential small API-like HTTP request/responses over selected TCP; steady request/response UDP at 64, 256, and 1200 payload bytes; an observational 1200-byte burst; and identical 256-byte UoT streams before and during the controlled 250 ms partition.
- Conflict scope: only the contract-matching `vpnctl-v2-gateway` and `vpnctl-v2-node` fixtures. No host route, host firewall, Homebrew package, foreign VM, public Telegram endpoint, credential, or real Clash Mi installation is a target. Existing Lima wildcard ignores continue to prevent host port forwarding.
- Automatic rollback: an EXIT trap terminates only its recorded background probe PID if still running, clears only the lab fault helper's exact nftables table/netem handle, deletes only the exact capture tables, and restores both runtime selectors. `./scripts/v2restricted-spike.sh stop` remains the listener/logging closure; `uninstall` remains the owner-checked guest rollback.
- Acceptance: healthy steady profiles and the post-fault probe must have no response loss; the impaired run must record at least one response over 100 ms and at least 100 ms maximum-RTT inflation over baseline. The burst is observational, not an SLA. The report must state that Telegram Bot API/webhooks are TCP/HTTPS, that the synthetic workload is shape-only, and that restricted UoT has no performance guarantee for voice, gaming, QUIC, bulk/high-rate UDP, adverse paths, or payloads above the tested 1200 bytes.
- Preflight result: both exact lab VMs were `Running`, x86_64/QEMU, 1 vCPU, 512 MiB, and 10 GiB; all four spike services were inactive; no capture/fault table and no development-host listener on the five ignored ports existed. `realty-front-docker-vm` was read only and remained outside the command target.
- Installation result: owner validation passed, both pinned Mihomo configs validated, and only the role-appropriate four spike units became active. Host `lsof` remained empty for TCP/UDP `1053`, `8443`, `17890`, `18080`, and `19090`.
- Benchmark result: the final repeat at `artifacts/v2lab/restricted-spike/benchmark-final/summary.json` passed. All healthy 64/256/1200-byte steady profiles and post-fault recovery had zero loss. The 1200-byte/1000 pps observation lost 81.4%. A 250 ms partition raised the 256-byte/100 pps stream from 67.861 to 911.094 ms p95, increased maximum RTT by 828.218 ms, and lost 12% within the bounded receive window.
- Safety/resource result: the capture counted 2535 protected TCP packets and zero native/direct UDP. Final gateway/node Mihomo RSS was approximately 44/48 MiB; total guest RSS approximately 320/282 MiB, with 453 MiB usable RAM and 1 GiB swap on each guest.
- Rollback verification: both exact capture/fault tables and the `1abc:` netem handle were absent; selectors were `RESTRICTED → RESTRICTED-VALID` and `RESTRICTED-UDP → RESTRICTED`; peer packet loss returned to zero; no benchmark port appeared on the host. The four test units remain active only until the explicit logging/runtime closure below.
- Logging/runtime closure: `./scripts/v2restricted-spike.sh stop` stopped all four exact spike units. Final `systemctl show` reported every unit inactive on both roles; the fault/capture tables and `1abc:` handle remained absent; host `lsof` remained empty for every ignored port. Both exact lab VMs remain `Running` for task 2.5, with owner-marked files retained for reproducibility and `uninstall` available as the exact guest rollback.

## 2026-09-01 — IP-only nginx ingress spike

### Planned host-forwarding isolation

- Preflight observation: neither vpnctl lab guest listened on `443/TCP`, and neither rendered lab configuration had a port-443 forwarding rule. The development host already had an unrelated `*:443` listener owned by PID 64389, whose command and cwd identify `realty-front-docker-vm`; that process, VM, and its files are read-only observations and are not targets.
- Versioned change: add one exact Lima ignore entry for guest port 443 with `guestIP: 0.0.0.0`, `guestIPMustBeZero: false`, `proto: any`, and `ignore: true`, matching the already accepted wildcard isolation contract.
- Planned metadata mutation: with every spike unit inactive, contract-check and stop only `vpnctl-v2-gateway` and `vpnctl-v2-node`, append only the exact 443 ignore entry to each stopped VM through `limactl edit`, verify the rendered metadata, and restart only those two VM instances.
- Conflict rule: refuse duplicate/non-ignore port-443 entries, drifted VM contracts, active spike units, or any command target other than the two exact x86_64/QEMU lab names. The foreign host listener may remain present throughout and must keep the same owner command.
- Rollback: stop only the two exact lab VMs, delete only their exact port-forward object whose `guestPort` is 443 and `ignore` is true, verify no other rule changed, then restart. Recreating only these lab VMs from the versioned template is the clean full rollback.
- Applied isolation: contract and inactive-unit checks passed; only the two exact lab VMs were stopped. One exact 443 ignore object was appended to each stopped VM, verified before restart, and both instances returned `Running` with evidence at `artifacts/v2lab/20260901-ingress-forwarding-isolated/summary.json`.
- Foreign-listener verification: after restart, the sole host TCP/443 listener remained PID 64389 with the `realty-front-docker-vm` hostagent command. Neither vpnctl lab hostagent created or acquired a host-443 listener.

### Planned nginx/package and certificate mutations

- Package scope: after forwarding isolation, refresh apt metadata only in `vpnctl-v2-gateway`; install the Ubuntu 24.04 nginx package only if absent. Prevent the distro `nginx.service` from starting during installation, then leave it disabled while the separately named spike unit runs the packaged binary with an owned configuration tree. Record exact before/after package versions for owner-checked uninstall. No host package manager is used.
- Guest resources: `/etc/vpnctl-v2-spike/ingress/` with its own owner marker and root-only RSA key, `/usr/local/libexec/vpnctl-v2-spike/webhook-receiver`, `vpnctl-v2-spike-ingress.service`, `vpnctl-v2-spike-webhook.service`, their exact runtime/state paths, gateway `443/TCP`, and loopback-only upstream `18081/TCP`.
- Certificate scope: generate RSA-2048/SHA-256 for the manually supplied IPv4, with IP SAN and compatibility CN, 1825-day validity, public certificate mode `0644`, and private key mode `0600`. The private key remains inside the gateway fixture and is never copied into evidence or command output.
- Test scope: validate nginx configuration; TLS 1.2 and 1.3; certificate algorithm, IP SAN/CN, and five-year window; real HTTP/1.1 and HTTP/2 POST forwarding to the loopback receiver; and unknown-path `404`. Access logging is off and the receiver records neither bodies nor paths.
- External gate: the local fixture cannot receive Telegram traffic because its address is private. A separate test-only helper may read a bot token from a hidden TTY, upload the public certificate as multipart data, validate `getWebhookInfo`, and remove the webhook, but no token may enter argv, environment, files, logs, JSON, or git. Task 2.5 remains incomplete until that flow runs against a manually supplied public gateway IP or is explicitly reassigned to the deployed-service release gate.
- Rollback: stop only the two ingress spike units, remove only owner-marked ingress files/units/state, restore prior package state if this spike installed nginx, and retain only ignored sanitized evidence. VM-level rollback remains `scripts/v2lab.sh down`; destroying the exact two lab VMs remains the full fallback.
- First prepare result: apt installed only `nginx` and `nginx-common` at pinned version `1.24.0-2ubuntu7.17`; the temporary mask prevented the distro unit from starting, and it ended disabled/inactive. RSA key/certificate and owner-marked fixtures were installed. Manual config validation then failed before either spike unit started because `/run/vpnctl-v2-spike-ingress` did not yet exist for the configured pid path.
- Failure state: `NGINX_INSTALLED_BY_SPIKE=true` records package ownership, the private key remains mode `0600` in the gateway fixture, no ingress listener started, and no Telegram/public endpoint was contacted. The package mask cleanup trap ran.
- Corrective action: create only the exact runtime directory with mode `0750` immediately before manual `nginx -t`. The systemd unit already declares the same `RuntimeDirectory`; uninstall removes the directory only if empty. Rerun must reuse the owned package and certificate rather than reinstall or rotate them.
- Second prepare result: reused the exact package and certificate, passed `nginx -t`, and started only `vpnctl-v2-spike-{webhook,ingress}.service`. Gateway listeners were wildcard `443/TCP` and loopback `18081/TCP`; `443/UDP` was absent. Distro `nginx.service` remained disabled/inactive, and the sole host TCP/443 listener retained foreign PID 64389.
- First local verify result: stopped before any TLS/HTTP request because host OpenSSL rendered the subject as `subject=CN=192.168.104.1`, while the text assertion expected spaces around `=`. Public certificate evidence was written, node temporary files were removed by the trap, services remained unchanged, and no private key was copied.
- Corrective action: request RFC2253 subject formatting explicitly and compare the exact stable `subject=CN=<manual-ip>` form. No certificate rotation, config reload, package, listener, or external request is needed.
- Final local E2E: `artifacts/v2lab/ingress-spike/evidence-local-final/summary.json` passed nginx validation, RSA-2048/SHA-256, exact 1825-day lifetime, IPv4 SAN/CN verification, TLS 1.2 and 1.3, HTTP/1.1 and HTTP/2 synthetic POST forwarding, unknown-path `404`, `443/TCP`, and closed `443/UDP`.
- Resource point: final gateway nginx master/worker RSS was approximately 8/8 MiB and the test-only Python receiver approximately 20 MiB; total gateway/node guest RSS was approximately 285/284 MiB. Both peer checks had zero loss. This is not the task 2.6 load result.
- Secret/isolation verification: evidence contains the public PEM only; scans found no private-key PEM. All exact node `/tmp/vpnctl-v2-ingress-*` files were absent. While guest nginx was active, host TCP/443 remained solely foreign PID 64389.
- Logging/runtime closure: `./scripts/v2ingress-spike.sh stop` stopped only the two ingress units. Both custom units and distro `nginx.service` are inactive/disabled; guest `443` and `18081` listeners are absent. Pinned packages and owner-marked certificate/config remain for a repeat or exact `uninstall`; the external Telegram gate has not run.
- External-gate preparation: installed the test-only `telegram-webhook-gate` in the owner path and added a loopback-only accepted-request counter. The helper requires a global manual IPv4, reads the bot token with a hidden TTY, refuses an existing webhook, uploads only public PEM, observes a real counter increase, and deletes only its own registration. Static tests forbid argv/env/file token inputs and sensitive output.
- Final local rerun: updated receiver/helper files were installed without package or certificate rotation; local TLS/HTTP verification passed again with `accepted_request_delta=2`; `stop` then left every nginx/ingress unit inactive and both guest listeners absent. No real Telegram API call or external bot state mutation occurred.
- Uninstall verification: owner check passed; only `nginx` and `nginx-common` version `1.24.0-2ubuntu7.17` were purged; exact ingress units, config, certificate/key, receiver/helper, and empty runtime path were removed. Restricted spike ownership/files remained intact, both lab VMs remained `Running` with the 443 ignore contract, and foreign host PID 64389 remained the sole TCP/443 listener. Apt metadata and sanitized ignored evidence were intentionally retained.
- Final lab state: task 2.5 local runtime is fully rolled back and inactive. A future local rerun or task 2.6 will reinstall from the pinned manifest; the real Telegram gate requires an actually deployed endpoint and has not run.

## 2026-09-02 — IP-only nginx ingress stress gate

### Planned reversible lab mutations

- Decision boundary: the user moved the real Telegram `setWebhook`/incoming-request check to deployed-service release task 16.11. Task 2.5 is complete on its local development evidence; no Telegram API or bot state is touched by task 2.6.
- Target scope: only the contract-matching `vpnctl-v2-gateway` and `vpnctl-v2-node` Lima fixtures. The unrelated host TCP/443 listener owned by `realty-front-docker-vm`, its VM, routes, files, and processes remain read-only observations and are never command targets.
- Package/config mutation: owner-checked `prepare` may reinstall only pinned Ubuntu `nginx`/`nginx-common` `1.24.0-2ubuntu7.17` in the gateway fixture, recreate only `/etc/vpnctl-v2-spike/ingress`, the two exact spike units, and `/usr/local/libexec/vpnctl-v2-spike` helpers. The distro nginx unit remains disabled/inactive.
- Runtime mutation: run bounded TLS requests from the node fixture to guest-only `443/TCP`; stream a 3 MiB synthetic body; load 40 safe, 45 single-expose overload, and 72 two-expose gateway-overload requests; temporarily replace only the owned generation include with generation B for one graceful reload; and restore generation A immediately afterward.
- Observability: access/error/body logging remains off. A bounded root helper polls only the two declared nginx temp directories and writes one sanitized JSON result outside them. Resource evidence contains process/cgroup counters, no request body, no token, no private key, and no sensitive generated path.
- Acceptance candidate: 64 gateway requests, 40 per expose, 8 MiB gateway body maximum, 1 MiB default body, 15-second default and 60-second maximum upstream timeout, 64 HTTP/2 streams, and 10-second graceful drain. The candidate is accepted only if streaming reaches upstream before upload completion, zero body temp files are observed, safe load is all `200`, excess load is shed as `503`, `404/413/503/504` are exact, graceful reload preserves the old request, and both cgroups record zero OOM events.
- Automatic cleanup: the stress trap kills only recorded local `limactl` jobs, removes the two exact node `/tmp` inputs and monitor output, atomically restores generation A, and reloads only the owned ingress unit. Explicit `stop` closes the two spike services; owner-checked `uninstall` removes only the recorded packages/files/units and private fixture key.
- Full rollback: `./scripts/v2ingress-spike.sh uninstall`. If fixture ownership validation fails, stop without deletion and recreate only the two exact lab VMs from the versioned template. The port-443 Lima ignore remains an intentional lab-safety control rather than stress-test state.
- First prepare result: pinned packages and a new root-only fixture certificate were installed and recorded as spike-owned, but manual `nginx -t` rejected a duplicate `proxy_read_timeout` produced by including defaults and overriding the same directive in one location. Validation failed before either custom unit or guest listener started; the package mask cleanup completed and no public/Telegram endpoint was contacted.
- Corrective action: move the 2/15/15-second proxy connect/send/read defaults to the parent `http` context and keep only the narrower 1-second directive in the child timeout location. Rerun must reuse the exact owner-marked packages and certificate and still pass full config validation before service restart.
- Second prepare and baseline result: the corrected config passed `nginx -t`, only the two custom units started, and the full task 2.5 TLS 1.2/1.3 plus HTTP/1.1/2 regression passed at `artifacts/v2lab/ingress-spike/pre-stress-regression/summary.json`.
- First stress result: 3 MiB streaming reached the upstream before upload completion with zero regular files across 1237 temp-directory samples; default/hard body checks returned `413`, unavailable returned `503`, timeout returned `504`, 40 safe requests were all `200`, and a 45-request single-expose overload produced exactly 40×`200` plus 5×`503`.
- First stress failure: the 72-request two-expose load returned 72×`200` instead of enforcing the gateway hard cap. Point evidence showed approximately 4.7 MiB ingress and 15.7 MiB receiver cgroup memory with 287 MiB guest memory available; no resource failure occurred. The EXIT trap restored generation A and removed exact temporary inputs.
- Root cause and corrective action: nginx does not inherit a parent `limit_conn` when the child context declares any `limit_conn`. The per-expose declaration therefore suppressed the server hard cap. The renderer fixture must emit both gateway and expose limits in every proxied location; a contract assertion now fixes the expected eight gateway-limit copies (server plus seven test locations) before rerun.
- Third prepare result: manual `nginx -t` accepted the two-scope limit config, but restarting the previously active unit removed/recreated its systemd `RuntimeDirectory`; sandboxed `ExecStartPre` then could not `chown` nginx temp subdirectories to worker user `nobody` because `CAP_CHOWN` was absent. The ingress unit exhausted its five bounded restart attempts and stopped failed with no `443/TCP` listener; the webhook fixture remained independently supervised.
- Corrective action: add only `CAP_CHOWN` to the ingress unit's existing `CAP_NET_BIND_SERVICE`/`CAP_SETGID`/`CAP_SETUID` bounding set, which nginx needs to prepare worker-owned temp directories after boot/restart. `prepare` now resets the failed state of only the two exact spike units immediately before restart. No global systemd state or distro nginx unit is changed.
- Fourth prepare preflight result: it refused before mutation because the independently healthy owned webhook unit still held loopback `18081/TCP` while ingress was failed; the old helper associated both ports only with ingress activity.
- Corrective action: bind the occupied-port ownership checks explicitly to `443 → vpnctl-v2-spike-ingress.service` and `18081 → vpnctl-v2-spike-webhook.service`. A port is reusable only while its exact owner unit is active; every unowned listener remains a hard conflict.
- Final prepare result: the exact-unit ownership check passed, `CAP_CHOWN` allowed restart-time temp-directory preparation, `nginx -t` passed, and only the two custom units became active. The distro nginx unit stayed disabled/inactive.
- Final stress result: `artifacts/v2lab/ingress-spike/stress-final-v2/summary.json` passed. The 3 MiB upload reached upstream before completion with zero body files; safe concurrency was 40×`200`; per-expose overload was 40×`200` plus 5×`503`; gateway overload was 64×`200` plus 8×`503`; and error semantics were exact `404`/`413`/`503`/`504`.
- Reload/resource result: the generation-A in-flight request completed while a new request used generation B, the nginx master PID stayed stable, and generation A was restored. Ingress/test-receiver cgroup peaks were 6,025,216/14,802,944 bytes with zero OOM events; hard-load `MemAvailable` was 290,840 KiB with 1,019,484 KiB swap free. Both peer checks had zero loss.
- Provider decision: accept pinned nginx as the development ingress provider with hard/default limits recorded in the manifest and design. Caddy fallback is not activated. Production-ready status remains blocked on the real Telegram provider flow in task 16.11.
- Runtime closure: `stop` stopped only the two spike units; owner-checked `uninstall` purged only spike-recorded `nginx`/`nginx-common`, removed exact units/config/certificate/key/helpers, and removed the two node temp inputs. Post-checks found no package, ingress path, custom active unit, guest `443/18081` listener, or temp input.
- Conflict/host closure: both vpnctl lab VMs remain `Running`; the intentional port-443 ignore remains in their metadata. The sole development-host TCP/443 listener remains foreign PID 64389 for `realty-front-docker-vm`, which was never mutated. Apt metadata and sanitized ignored evidence remain intentionally.

## 2026-09-02 — multiplexed reverse-tunnel frp spike

### Planned reversible lab mutations

- Decision boundary: task 2.7 evaluates pinned frp as a replaceable development tunnel provider. It does not install production vpnctl services, publish a public listener, change the public expose API, or make frp a user-visible contract.
- Exact targets: only the contract-matching `vpnctl-v2-gateway` and `vpnctl-v2-node` Lima fixtures. The foreign `realty-front-docker-vm`, development-host firewall/routes/packages/listeners, public services, and all non-vpnctl VMs are read-only and never command targets.
- Forwarding isolation: add exact wildcard Lima ignore entries for guest ports `17000`, `17400`, `18111`, `18112`, `18121`, `18122`, and `19091` to both versioned and existing fixture metadata while all spike units are inactive. Stop and restart only those two verified fixtures. Refuse duplicate, non-ignore, or drifted entries. The clean rollback is to stop the same fixtures, remove only those seven exact ignore objects, verify all other metadata is unchanged, and restart; recreating only the two fixtures from the versioned template is the full fallback. Keeping the ignores is preferred because it prevents accidental host exposure in later repeats.
- Pinned artifact: download only official `frp_0.69.0_linux_amd64.tar.gz` into ignored `artifacts/v2lab/cache/`, require SHA-256 `6b90d1cd28fc661f170c0de90dde03d2c63e4fd7ce0ae2da2ca1c28014b8146e`, and install only the extracted `frps`/`frpc` binaries inside the two fixtures. The exact cache directory can be removed manually after review; no host package manager is used.
- Gateway-owned resources: `/etc/vpnctl-v2-spike/tunnel/` with owner marker, self-signed tunnel leaf/key, shared frp bootstrap token, `frps.toml`, `vpnctl-v2-spike-tunnel-{auth,server}.service`, `/usr/local/libexec/vpnctl-v2-spike/{frps,tunnel-auth-plugin}`, and `/var/lib/vpnctl-v2-spike-tunnel-auth/`. Listener scope is gateway-lab-only `17000/TCP`, gateway-loopback proxy endpoints `18111/18112`, and gateway-loopback plugin `19091/TCP`; frps dashboard, HTTP/HTTPS vhosts, KCP, QUIC, P2P, and public proxy binds stay disabled.
- Node-owned resources: the same owner-scoped config root, root-only tunnel credential/config/bootstrap token, public tunnel certificate, `vpnctl-v2-spike-tunnel-{client,backend}.service`, `/usr/local/libexec/vpnctl-v2-spike/{frpc,tunnel-backend,tunnel-probe}`, loopback admin `17400/TCP`, and loopback application fixtures `18121/18122`. No node inbound firewall opening or non-loopback listener is created.
- Secret handling: generate one test-only random 256-bit node tunnel credential plus independent bootstrap/admin values under mode-`0700/0600` ignored artifacts, copy them only through root-owned temporary/config files, and remove guest `/tmp` copies. The authorization state stores only the tunnel-token SHA-256. Raw values, private keys, webhook paths/bodies, and authorization metadata must not enter evidence, argv output, logs, status, git, or this journal.
- Temporary logging opt-in: the disposable frp units use `info` journal output only for reconnect timing and plugin-behavior evidence; the plugin and backend disable request/access logging and never record metadata or payloads. `stop` closes these units and therefore ends this explicit diagnostic window. Production defaults remain no logging without a separate temporary opt-in.
- Verification mutations: atomically switch only the owned `frpc.toml`/proxy include; call loopback-only `frpc verify/reload`; run bounded loopback streams; stop/start only the owned frps unit for reconnect; toggle only the test node's authorization state for revoke; and create/delete only nftables table `inet vpnctl_v2_spike_tunnel_capture` on the node fixture.
- Real transport switch: if the previously owner-marked restricted spike is clean, start its exact four services through `scripts/v2restricted-spike.sh prepare`, save its Mihomo mode/selector state, temporarily select valid restricted transport plus global routing, and restart only the tunnel frpc with `proxyURL=socks5://127.0.0.1:17890`. Counters must show direct `gateway:17000` before the switch and only ShadowTLS outer `gateway:8443` afterward. Always restore the original Mihomo mode/selectors, remove the exact capture table, restore standard frpc config, and stop restricted services only when task 2.7 started them.
- Acceptance: TLS identity failure occurs before Login; Login rejects inactive/old-generation credentials and any pool input other than the pinned v0.69.0 normalized value, then rewrites that value to effective zero; NewProxy rejects an unregistered name/type/port while preserving valid mappings; controller/plugin unavailability denies new mappings; add/remove reload keeps the frpc PID and the one control socket; concurrent streams for both exposes use one persistent node-to-frps TCP connection; frps restart recovers without frpc restart; revoke closes the current tunnel within the measured heartbeat bound and blocks reconnect; the restricted switch preserves node identity/expose ownership; both 512 MiB guests retain safe memory headroom with zero unit OOM events.
- Automatic and explicit rollback: verification traps restore active authorization, standard frpc config, Mihomo state, restricted-unit prior state, and the exact capture table. `./scripts/v2tunnel-spike.sh stop` stops only the four tunnel units. Owner-checked `uninstall` removes only the listed tunnel files, units, state, binaries, private fixture key, and temporary inputs; it refuses deletion on marker mismatch. Destroying only the two lab fixtures is the final clean fallback.
- First isolation attempt: read-only preflight confirmed both exact fixtures `Running` with the required QEMU/x86_64/1-vCPU/512-MiB/10-GiB/image/network contract, all prior and tunnel spike units inactive, all seven new guest ports free, no existing matching metadata, and no development-host `17000/TCP` listener. Both exact fixtures then stopped cleanly and accepted the seven exact ignore objects.
- Verification-script failure: the first post-edit `jq` expression changed input context from the VM object to a numeric port inside `all()`, so it attempted `.config` on a number and aborted before restart/report. The EXIT safety trap requested startup of both exact fixtures. This was a read-only assertion defect after both metadata edits, not a guest/config partial write; no frp artifact, unit, credential, listener, nftables rule, or host service was installed or started.
- Corrective action: do not repeat metadata mutation. Wait for both safety-trap startups, then validate each stopped/started instance with a context-preserving `jq` binding, require exactly one matching ignore object per requested port, require the original VM contract, and produce the isolation report. Any duplicate or drift remains a hard stop before frp installation.
- Corrected isolation result: gateway was already `Running`; the first best-effort trap start left node `Stopped`, so only `vpnctl-v2-node` was started once more. Both fixtures then reported `Running`, preserved the full VM/image/network contract, and contained exactly one matching wildcard-ignore object for every one of the seven ports. `artifacts/v2lab/20260902-tunnel-forwarding-isolated/summary.json` passed, the resource baseline was captured, and development-host `17000/TCP` remained free. No metadata edit was repeated.
- First prepare result: the official archive downloaded completely and matched the pinned SHA-256, and root-only ignored credentials/configs were generated. Preparation then stopped before copying or installing any guest file because the host-side version gate tried to execute the Linux/amd64 binary on the macOS/arm64 development host and received `cannot execute binary file`. No tunnel unit, listener, nftables rule, certificate, or guest credential exists from this attempt; the verified archive and ignored generated artifacts are safe to reuse or remove by their exact paths.
- Corrective action: host-side validation remains archive SHA-256 plus expected layout only. Execute `frps --version` and `frpc --version` immediately after their exact guest installs on the amd64 fixtures, before config validation or unit start. A mismatch remains a hard stop.
- Second prepare result: the corrected architecture gate passed inside both amd64 guests and installed only the planned owner-marked tunnel resources. The four exact units started with temporary `info` logging: gateway authorization/plugin and frps listened only on `127.0.0.1:19091` and the gateway overlay IP on `17000`; node backend/admin listened only on `127.0.0.1:18121/18122/17400`. No expose listener appeared because every frpc Login was fail-closed by the local authorizer with sanitized reason `pool_not_zero`; no secret or metadata value entered the metric. The node had no capture table, and no verification mutation beyond startup ran.
- Root cause: pinned frp `v0.69.0` normalizes client `transport.poolCount = 0` to Login `pool_count = 1` in `ClientCommonConfig.Complete`, despite the full example describing zero as the default. Using `-1` is unsafe: server `BaseProxy.GetWorkConnFromPool` loops `poolCount + 1` times, so a negative value can return a nil work connection. The supported Login-plugin response can instead authorize the expected pinned input and replace only `pool_count` with zero before frps creates the control session; server `Control.Start` then preloads no work connection while on-demand streams still use tcpMux.
- Corrective action: keep the owned client declaration at zero for intent, require the pinned v0.69.0 normalized Login value to be exactly one, and have the local authorizer return an otherwise identical Login with `pool_count = 0`. Reject every other incoming value, cover the pinned normalization workaround with a regression contract, and prove in runtime evidence that the resulting session has one physical tunnel connection and no idle work stream. Re-running owner-safe `prepare` may replace only the previously listed owned files and restart only the four exact units.
- Third prepare result: the version-pinned Login rewrite passed both frps/frpc config validation and the initial end-to-end loopback probe. All four exact units are active: authorizer `10.4 MiB`, frps `2.8 MiB`, frpc `2.2 MiB`, and backend `5.9 MiB` at the status snapshot. Listeners are limited to gateway overlay `17000/TCP`, gateway loopback `19091/18111`, and node loopback `17400/18121/18122`; `18112` remains intentionally absent until dynamic-add verification. The node capture table is absent and the prepared script confirmed development-host `17000/TCP` remains unforwarded.
- First acceptance result: both dynamic add/remove reload cycles passed config verification and reported `reload success`; concurrent stream evidence was written. The malicious mapping was rejected as `mapping_mismatch`. The stale-generation mapping was also rejected—the NewProxy reject counter advanced from one to two—but a one-second authorized Ping replaced the global `last_event` before the script read it, so the assertion incorrectly reported that no stale mapping reject occurred and stopped before lifecycle/transport tests.
- Acceptance rollback check: the trap restored the standard one-proxy config and active authorization, removed any exact capture table, and left all four exact units active. Only gateway loopback expose `18111` remains; `18112` and the node capture table are absent. No restricted transport state had been entered in this run.
- Corrective action: retain aggregate counters but add a sanitized `last_by_operation` metric keyed by operation. Negative assertions read their own operation record so heartbeat interleaving cannot hide a NewProxy/Login decision. Reinstall only the owned authorizer through owner-safe `prepare`, then repeat the full acceptance run.
- Second acceptance result: dynamic reload, concurrent streams, malicious/stale mapping rejection, untrusted TLS, old generation, unexpected pool input, frps reconnect, and bounded revoke/reconnect all passed and the run reached the real restricted transport. The restricted fixture was cleanly prepared, both Mihomo configs validated, and ShadowTLS outer traffic appeared on `8443/TCP` (`46` packets). The assertion stopped because the same snapshot also contained `6` direct `17000/TCP` packets emitted while the old standard frpc connection was being closed during the switch; the script had reset counters before, rather than after, replacing that connection. Local frpc-to-Mihomo count was computed but not yet persisted before the stop.
- Transport rollback check: the trap restored active authorization and the standard direct frpc config, removed the exact capture table, and stopped the four restricted units that this run had started. Both authorized mappings remain active under standard transport; the four tunnel units are active, their expected overlay/loopback listeners are present, and no capture table remains.
- Corrective action: treat connection replacement as a transition phase. Select restricted routing, restart frpc, wait until both mappings and exactly one frpc-to-loopback-Mihomo connection are established, then recreate the exact capture table to zero transition packets. Generate fresh expose traffic and require steady-state `17000 = 0`, `8443 > 0`; persist the three sanitized counts before asserting.
- Final acceptance result: `artifacts/v2lab/tunnel-spike/evidence-20260901T215438Z/summary.json` passed. One persistent tcpMux connection carried `12` concurrent streams for each of two exposes with effective pool zero; add/remove used reload without frpc PID replacement. TLS identity failed before Login, generation/pool/mapping negatives were rejected, and revocation closed the tunnel in `2s` and denied reconnect. A frps restart recovered in `8s` without restarting frpc. Standard capture saw `59` direct `17000/TCP` packets; steady restricted capture recorded exactly one frpc-to-Mihomo connection, zero direct `17000` packets, and `22` ShadowTLS outer `8443/TCP` packets while preserving node/mapping identity.
- Resource result: on the exact 1-vCPU, 512-MiB, 10-GiB fixtures with the accepted 1-GiB swap, gateway/node `MemAvailable` remained `279028/292420 KiB`; no tested unit recorded an OOM kill. The sanitized resource report is under the same evidence directory.
- Provider decision: accept pinned frp `v0.69.0` as the replaceable development reverse-tunnel provider, with one shared internal-only frps and one frpc per node. The v0.69.0 Login pool rewrite is mandatory and version-locked; provider upgrades must re-run this spike/contract before activation. OpenSSH remains the documented fallback if the pinned provider contract stops passing.
- Runtime closure: cleanup restored standard direct transport and active authorization, removed the exact capture table, and stopped only the restricted units started by verification. Final read-only status found all four tunnel units active with only the expected overlay/loopback listeners; all four restricted units are inactive. The temporary frp logging opt-in remains open only while these disposable tunnel units are intentionally active and can be closed with `./scripts/v2tunnel-spike.sh stop`.
- Additional fail-closed verification plan: the first passing evidence proves credential/mapping rejection but does not exercise an unreadable controller state. Extend the already armed verification trap so it restores an exact owner-scoped `/var/lib/vpnctl-v2-spike-tunnel-auth/state.json.unavailable` back to `state.json` only when the original is absent. During verification, remove the second mapping, temporarily rename only that state file, attempt to add the second mapping, require a sanitized NewProxy `controller_error` reject and no second listener, restore the file, and re-add both mappings. No service, public endpoint, credential, or foreign path is changed by this test.
- First controller-unavailable result: frpc reload re-announced the full configured mapping set, so fail-closed authorization temporarily removed the existing first listener as well as rejecting the new second listener. The script's extra availability probe therefore received `connection refused` before it persisted the operation metric. The EXIT trap restored the exact state filename and standard config; final status showed both authorized mappings and all four tunnel units healthy, no `.unavailable` file, and no capture table.
- Corrective action: availability during a management mutation with unavailable authoritative state is not promised; safety requires denying every re-announced mapping. Wait for the NewProxy reject counter to advance, persist and require per-operation `controller_error`, require only that the unauthorized second listener remains absent, then restore state and explicitly render one followed by two mappings before continuing.
- Extended acceptance result: `artifacts/v2lab/tunnel-spike/evidence-20260901T220258Z/summary.json` supersedes the earlier passing run and includes `controller_unavailable_rejected = true`. With the exact state filename hidden, sanitized per-operation metrics recorded `controller_error` for Login, NewProxy, and Ping; no unauthorized second listener appeared. The trap-safe explicit restore then re-authorized both mappings and the remaining gate passed: one persistent connection, 12 streams per expose, reconnect in `7s`, revoke in `2s`, standard direct `51` packets, and restricted steady-state exactly one local proxy connection, zero direct packets, and `22` ShadowTLS packets. Gateway/node `MemAvailable` was `277324/287932 KiB`, with zero unit OOM kills.
- Extended runtime closure: final read-only checks found the authoritative `state.json` present and no `.unavailable` sibling, all four tunnel units active on only their expected overlay/loopback listeners, both authorized mappings restored, no capture table, and all four restricted units inactive.
- Logging/runtime closure: after preserving final evidence, `./scripts/v2tunnel-spike.sh stop` stopped only the four owner-marked tunnel units. All four are now `inactive/dead` with no tunnel/backend/plugin/admin listener and no capture table; the temporary frp `info` logging opt-in is closed. Owner-marked inactive files and ignored evidence remain for an exact repeat or owner-checked uninstall.

## 2026-09-02 — fail-closed routing and nftables spike

### Planned reversible lab mutations

- Decision boundary: task 2.8 selects internal nftables hook priorities, mark bits, policy-table IDs, readiness ordering, and rollback behavior. It does not install production vpnctl networking or change the VM root namespace routes/firewall/sysctls.
- Exact target: only the contract-matching `vpnctl-v2-node` Lima fixture. The gateway fixture, development host, foreign `realty-front-docker-vm`, public endpoints, root-namespace Lima `ip nat` table, and all non-vpnctl services are read-only controls.
- Isolation topology: create only network namespaces `vpnctl-v2-rnode`, `vpnctl-v2-rdirect`, and `vpnctl-v2-rgateway` plus their owner-named veth pairs. The simulated node has independent direct and gateway links, duplicate selected IPv4/IPv6 targets on the two peer paths, a node-local ingress fixture, and an unrelated direct target. Namespace deletion is the full topology rollback.
- Candidate mark namespace: reserve only high byte mask `0xff000000`: direct `0x01000000`, selected `0x02000000`, recovery/active-outbound `0x03000000`, and ingress-response `0x04000000`. Preserve the lower 24 bits and refuse overlapping existing rules. Candidate RPDB priorities are `10000/10010/10020`; selected table is `20001` with a persistent unreachable default plus a lower-metric managed-TUN route only while ready, and gateway/recovery table is `20002`.
- Candidate nftables scope: own only `inet vpnctl_v2_spike_routing` inside the simulated-node namespace. Prerouting and route-output chains use priority `mangle` (`-150`), after conntrack association at `-200`. Conntrack marks retain direct/selected/recovery/ingress decisions. An atomically replaced readiness chain permits only loopback, retained established-direct flows, ingress responses, and exact gateway recovery traffic while not ready; selected IPv6 is always rejected because v2 lacks a full IPv6 data plane.
- TUN/provider fixture: install a routing-owned copy of the already pinned Mihomo `v1.19.30`, configured silent with `auto-route: false`, one real TUN, and a direct test outbound bound/marked to the isolated gateway link. It exists only to exercise kernel hooks and readiness; task 2.8 does not change the transport provider decision.
- Owner-scoped files/services: `/etc/vpnctl-v2-spike/routing/`, `/run/vpnctl-v2-spike-routing/`, `/usr/local/libexec/vpnctl-v2-spike-routing/`, and exact `vpnctl-v2-spike-routing-{guard,engine,direct,gateway,node}.service` units. Python fixtures disable request/access logging; Mihomo uses silent logging. No temporary logging opt-in is opened.
- Fault acceptance: inject failure after policy rules but before activation and require exact rollback; boot guard before TUN; verify ready selected TCP/UDP uses only the gateway path while unmatched IPv4/IPv6 remains direct; verify selected IPv6 blocks; prove lower foreign mark bits and a foreign nftables table/rule survive; prove gateway-originated ingress replies return through the gateway link; crash/restart the engine while retained established-direct traffic continues and every new application flow blocks except recovery; require no direct selected response during restart; then remove only vpnctl policy and prove ordinary networking plus foreign resources are restored.
- Automatic and explicit rollback: every verification mutation arms an EXIT trap that restores readiness state, stops only exact units, removes only exact vpnctl rules/routes/table/sysctls, and deletes only the three spike namespaces. Owner-checked `stop` leaves the fail-closed guard by design; `uninstall` first restores ordinary networking, then removes only exact files/units/namespaces. A marker mismatch or foreign overlapping mark/rule/table is a hard stop.
- First prepare result: the pinned binary and owner-scoped files installed, topology creation completed, and both Mihomo config validation and all five units remained before activation. The subsequent `nft -c` preflight rejected two expressions that attempted to OR lower packet-mark bits with dynamic upper conntrack-mark bits; nftables `1.0.9` requires the right side of that binary operation to be constant. No guard table, policy rule, TUN, backend listener, or service became active.
- First rollback observation: all five exact units are `inactive/dead`, but the ERR cleanup left the three isolated namespaces present and `status` could not render absent policy-table JSON. The VM root namespace and foreign services remain outside the mutation scope. Before any retry, correct status handling for absent FIB tables, inspect the owner namespaces read-only, then use only the exact owner-checked uninstall/cleanup path; do not create a second topology over this state.
- Corrective design: when a connection is first classified, compose the high vpnctl byte into the current packet mark and then copy the full packet mark into `ct mark`, retaining all lower 24 bits. On later packets, match only the four exact vpnctl high-byte values and restore the full saved conntrack mark. This avoids unsupported dynamic bitwise OR, preserves the lower mark namespace across conntrack, and does not claim arbitrary foreign high-byte marks.
- First rollback completion: read-only inspection found no process in any spike namespace, no vpnctl nftables table/rule/route, and only the deliberately foreign fixture inside the isolated node namespace. Owner-checked `uninstall` then removed the three exact namespaces, five units, and owned file roots. The post-check reports every namespace absent and every unit `not-found/inactive/dead`.
- Rollback/status correction before retry: replace the function-scoped `ERR` trap with an armed `EXIT` trap immediately before topology creation, and disarm it only after the entire prepare succeeds. Treat a nonexistent candidate FIB table as an empty JSON array in preflight/status/clean assertions. Local shell/Python/JSON checks and the routing regression contract pass with these changes.
- Second prepare result: nftables syntax/config preflight passed the corrected mark expressions. The JSON helper still combined iproute2's partial `[` output for an absent table with the fallback `[]`, so its `jq length` check was invalid but non-fatal in the conditional. Preparation then stopped before service start because exact `systemctl reset-failed <name...>` reports a never-loaded unit as an error even when its valid unit file exists. No guard, TUN, backend, listener, or policy became active.
- Second rollback result: the new `EXIT` trap removed all three namespaces and runtime policy after the failure. Read-only post-check found all five valid owner unit files visible to systemd but `inactive/dead`; the owner marker remains exact. Correct the route helper to capture successful stdout before printing it and otherwise emit only `[]`; make reset of only the five exact unit names best-effort before their first start. Reusing the owner files is safe and creates no duplicate resources.
- Third prepare result: corrected policy/nft preflight passed, all three backend fixture units reached systemd `active`, and the first immediate direct-path probe raced listener-thread initialization and received `ECONNREFUSED`. The `EXIT` trap again removed all namespaces and stopped every exact unit; post-check shows all five `inactive/dead`, each backend `Result=success`, `ExecMainStatus=0`, and zero restarts. No guard or routing engine started.
- Third corrective action: unit activation alone is not endpoint readiness. Add a bounded, silent request loop for every baseline listener before declaring prepare complete. Also set `StandardOutput=null` and `StandardError=null` on all five disposable units so the agreed default remains no journald service output; acceptance evidence continues through bounded root-only JSON files and explicit command results.
- Fourth prepare result: full preparation passed. The three backend fixtures are `active/running` inside the exact namespaces, all required direct-path TCP/UDP/IPv4/IPv6 readiness probes answered, while guard and engine remain `inactive/dead`. Policy status is clean (`readiness=absent`, no vpnctl rules or candidate routes). Read-only systemd properties confirm `StandardOutput=null` and `StandardError=null` for every one of the five units. The VM root namespace remains the unmodified control snapshot for acceptance.
- Final acceptance result: `artifacts/v2lab/routing-spike/evidence-20260901T223531Z/summary.json` passed. Injected activation failure rolled back cleanly; boot guard blocked every new TCP/UDP IPv4/IPv6 application probe except the exact recovery target; ready selected TCP/UDP used only the gateway TUN path; unmatched IPv4/IPv6 stayed direct; selected IPv6 stayed blocked; lower foreign mark bits, the foreign nftables table, and foreign RPDB rule survived unchanged; and the gateway-originated ingress connection completed only through its symmetric gateway response route.
- Crash/restart result: an established direct TCP flow survived routing-engine `SIGKILL` through its retained conntrack decision, while new selected and direct application traffic blocked until automatic readiness returned. During manual restart, selected TCP recorded `156` gateway responses, `17` blocked attempts, and `0` forbidden direct responses; selected UDP recorded `220` gateway, `3783` blocked, and `0` forbidden; selected IPv6 recorded `20453` blocked and `0` direct responses.
- Final rollback result: verification removed vpnctl policy, proved ordinary selected/direct IPv4 and IPv6 topology behavior returned, and byte-compared the foreign nftables/rule fixtures before and after. Owner-checked full uninstall then removed only the three namespaces, five units, and three owned path roots. Post-check reports all namespaces absent, every unit `not-found/inactive/dead`, and byte-identical root nftables, IPv4/IPv6 rules, and all-table route snapshots. The host/gateway VM and foreign development-host listener were never mutated.
- Routing decision: accept mangle-priority `-150` prerouting/route-output hooks, high-byte mask `0xff000000` with direct/selected/recovery/ingress values `0x01/0x02/0x03/0x04 << 24`, RPDB priorities `10000/10010/10020`, selected/gateway tables `20001/20002`, persistent unreachable metric `42760`, and ready TUN metric `10` as conflict-checked internal defaults. Mihomo `v1.19.30` with `auto-route: false` is accepted for this routing fixture; DNS mode remains intentionally undecided until task 2.9.

## 2026-09-02 — systemd-resolved and split-DNS mode spike

### Planned reversible lab mutations

- Decision boundary: task 2.9 selects the internal Mihomo DNS mode and systemd-resolved integration shape. It does not implement production DNS commands, contact public resolvers, alter an actual Clash Mi installation, or satisfy the deployed-client gate in task 16.11.
- Exact target: only the contract-matching `vpnctl-v2-node` Lima fixture is mutable. The gateway fixture, development host, foreign `realty-front-docker-vm`, public DNS, and non-vpnctl services are read-only controls. No guest listener is exposed or auto-forwarded to the development host.
- Observed resolver baseline: Ubuntu systemd `255.4-1ubuntu8.15`, active `systemd-resolved`, `/etc/resolv.conf -> /run/systemd/resolve/stub-resolv.conf`, node `eth0` DNS `198.18.0.2`, route domain `~.`, default-route enabled, and only Lima's owned `table ip nat` in the root nftables ruleset. `/etc/systemd/resolved.conf` and Lima's `00-lima-enable-mdns.conf` hashes were captured read-only; no vpnctl DNS user, unit, path, or listener exists.
- Isolated upstream topology: create only `vpnctl-v2-dns-direct` and `vpnctl-v2-dns-gateway` namespaces plus root veths `v2dnsdirect0` and `v2dnsgateway0`. Deterministic no-log authoritative fixtures listen on TCP/UDP `53` at `10.211.0.1` and `10.212.0.1`, with bounded TTL and root-only aggregate counters. Namespace deletion is the full topology rollback.
- Resolver candidate: install the already pinned/checksummed Mihomo `v1.19.30` as a DNS-only root-loopback service on TCP/UDP `127.0.0.1:1053`, run it as a dedicated non-login `vpnctl-v2-dns-spike` system user, set `log-level: silent`, disable system-host recursion and external updates, and direct upstream sockets explicitly to one of the two veth interfaces. Every systemd unit has `StandardOutput=null`/`StandardError=null` and bounded memory/tasks.
- Candidate modes: compare policy `redir-host` with policy `fake-ip`; the fake candidate uses a whitelist so only selected domains receive fake addresses and unmatched names retain real direct addresses. The standard `198.18.0.0/16` range is rejected in this fixture because it contains the real Lima underlay resolver `198.18.0.2`; use conflict-checked `198.19.0.0/16` for the candidate. Compatibility `direct` uses `redir-host` and only the saved direct upstream path. Validate matching node and Clash-compatible profiles with the pinned binary, but defer actual Clash Mi import/behavior to task 16.11.
- Managed host integration: owner-create only `/etc/systemd/resolved.conf.d/vpnctl-v2-dns-spike.conf`, point global DNS to `127.0.0.1:1053`, clear fallback, route `~.` there, and disable systemd-resolved's second cache layer. Because the existing `eth0` also owns `~.`, snapshot its DNS/domain/default-route state and temporarily replace only its route-domain with an inert owner name after resolved restart so queries cannot race to underlay DNS. The routing guard is the production activation boundary; this disposable spike runs probes only after readiness.
- Classic DNS capture: create only `inet vpnctl_v2_spike_dns` in the node root namespace. Its output NAT chain excludes the resolver's exact UID and loopback systemd stubs, redirects every other local TCP/UDP destination port `53` to `1053`, and records named counters. A hardcoded nonexistent destination must still resolve through policy; the resolver UID must reach both real upstream fixtures without recursion. Foreign Lima nftables resources must remain byte-identical.
- Acceptance: prove `resolvectl`, libc/NSS, and direct `dig` clients use the managed path; selected/direct upstream counters never cross; gateway outage blocks fresh selected names while fresh direct names continue; a previously accepted answer is served only within bounded TTL and expires without cross-path fallback; classic UDP and TCP port `53` are captured; policy/direct mode switching is manual and atomic; fake-IP appears only for selected names; and every rendered profile passes pinned `mihomo -t`.
- Automatic and explicit rollback: verification arms an `EXIT` trap before any resolved/nft mutation. Restore the exact per-link DNS/domain/default-route values, remove only the owned resolved drop-in and nftables table, restart only `systemd-resolved`, stop/remove the three exact spike units, delete the two namespaces/veths, and delete the dedicated user only after marker/path/process checks. Compare resolver config hashes/symlink, resolved runtime state, root nftables, addresses/routes, and foreign services before/after. Deleting/recreating only `vpnctl-v2-node` from the versioned template is the final fallback.
- First prepare result: the pinned binary, six candidate/profile configs, helpers, three no-log units, owner marker, and dedicated non-login UID installed. Preparation returned non-zero before its success boundary with no service output by design. The armed cleanup left all three units `inactive/dead` with zero restarts, removed both namespaces, and preserved owner files/user for diagnosis. Read-only post-check confirms no resolved drop-in, no vpnctl DNS nftables table, and exact original `eth0` DNS `198.18.0.2`, domain `~.`, and default-route `yes`; systemd-resolved remains active on the original stub.
- Diagnostic boundary: do not repeat topology or service start yet. Run pinned config validation and nft preflight individually without activating DNS integration, identify the first rejected fixture, then update only the versioned owner files. A full owner-checked uninstall remains available if ownership or restoration checks drift.
- First diagnosis: all three installed node configs pass pinned `mihomo -t`, and the rendered nftables preflight passes. Directly opening root-only profile exports as the service UID correctly fails permission checks; the orchestrator is intended to copy each into a private validation workspace first. That workspace was below root-only `/run/vpnctl-v2-spike-dns`, so the service UID could not traverse its parent even though the leaf was user-owned. Move only the transient validation directory below the already user-owned `StateDirectory`; keep profile exports root-only. No topology/service/resolved/nft mutation is needed for this correction.
- Second prepare result: all pinned config/profile validations passed from the corrected StateDirectory, topology and both authoritative units reached systemd `active`, but the first bounded root-to-direct DNS readiness query did not return the expected A record. The `EXIT` trap stopped all units and removed both namespaces/veths. Post-check again shows no resolved drop-in/table, original link DNS state, and all units `inactive/dead` with success/zero-restart final state.
- Second diagnostic boundary: recreate only the two exact owner namespaces and authoritative units, inspect root route/UDP+TCP socket reachability, raw `dig` response status, and aggregate backend counter, then run exact cleanup before changing fixtures. Do not activate the local resolver, systemd-resolved integration, or nft capture during this diagnosis.
- Authoritative-unit diagnosis: the exact direct veth route resolved to `v2dnsdirect0`, ICMP reached `10.211.0.1` with zero loss, but the namespace had no TCP/UDP listener and `dig` received `connection refused`. The bounded shell trap stopped both units and deleted both namespaces. This isolates the failure to backend process startup or its systemd sandbox, not topology/routing or DNS response parsing.
- Next diagnostic boundary: run the same installed authoritative helper directly inside the exact namespace, probe UDP/TCP and its state file, terminate only its recorded PID, and delete the namespaces via the armed trap. If direct execution passes, inspect only systemd execution credentials/path permissions; if it fails, correct the Python fixture before another service start.
- Direct backend result: the same installed helper stayed alive in `vpnctl-v2-dns-direct`, bound both `10.211.0.1:53/UDP` and `/TCP`, returned `192.0.2.77` for both transports, and atomically recorded exactly two aggregate queries. The recorded PID was terminated and both namespaces were removed by the diagnostic trap. Python packet construction and filesystem state are accepted.
- Unit diagnostic boundary: recreate the exact topology, start only the direct unit, read `ExecMainCode`, `ExecMainStatus`, `Result`, namespace listener, and state-file metadata before cleanup. Continue without journal output; open a bounded temporary error-level opt-in only if systemd status cannot identify the sandbox failure.
- Single-unit result: the direct systemd unit reached `active/running` with `Result=success`, `ExecMainStatus=0`, zero restarts, both namespace listeners, and its root-only state file. The sandbox is valid. The failed readiness occurred only during simultaneous cold start of two Python fixtures on the emulated 1-vCPU guest.
- Corrective orchestration: start and prove the direct upstream first, then start and prove the gateway upstream, then start the local resolver. Extend the bounded endpoint loop from 20 to 30 seconds. Production units remain independently supervised; this only removes unnecessary cold-start contention from the minimum-host development gate.
- Third prepare result: sequential startup still timed out on the same direct readiness name, disproving cold-start contention as the cause. The cleanup again removed namespaces and left resolved/nft/link state exact. The successful manual query ran as root inside one Lima shell, while the orchestration probe launches `dig` as the ordinary Lima user through a separate shell; test that exact execution identity/path next before modifying service order again.
- Ordinary-user result: the exact unit plus topology returned `192.0.2.77` to the ordinary Lima UID, used the expected connected route/source, and incremented the UDP counter once. Execution identity and permissions are valid; the diagnostic trap stopped the unit and removed topology.
- Readiness correction: avoid sending repeated UDP queries before bind. Wait first for both exact TCP and UDP listeners through read-only `ss` in the target namespace/root, then issue one semantic DNS query. Apply the same two-phase readiness to direct, gateway, and local resolver starts/restarts.
- Fourth prepare result: listener-first guards passed far enough to reach a semantic readiness timeout, but the reused query name made the message ambiguous between the direct authoritative endpoint and the final local resolver endpoint. The EXIT cleanup restored the same clean state. Include endpoint coordinates in future readiness errors and run all three exact units without resolved/nft integration to inspect local resolver status plus which upstream counter receives its query.
- Full-chain diagnosis: with both exact upstreams and the local resolver started but no resolved/nft integration, the resolver remained `active/running` with both loopback listeners. A local query returned the direct fixture answer in `34ms`; direct recorded exactly one request and gateway zero. The diagnostic trap stopped all three units and deleted topology. Re-run owner-safe prepare with endpoint-specific errors before making any further design change.
- Fifth prepare result: endpoint-specific output proves the timeout is the first `10.211.0.1:53` semantic check, despite listener readiness. The host macOS `awk` expression accepts the expected IPv4 in isolation. The standard EXIT cleanup completed. Reproduce the exact host `node_shell → dig → awk` function once against the exact direct unit and persist unit/state output before trap cleanup; do not change DNS architecture.
- Exact-function result: after one second beyond socket bind, the unchanged host pipeline returned and parsed `192.0.2.77`; the direct unit was active and its counter advanced once. On the emulated single CPU, both Python threads can expose bound sockets before their receive loops are scheduled. Add a bounded one-second settle after listener readiness for each fixture/local resolver; retain listener and semantic checks as the actual gates.
- Sixth prepare result: the post-bind settle did not change the direct endpoint timeout, so scheduler contention is rejected as the cause. Cleanup remained exact. Add a failure-only, non-journal snapshot before exit: matching unit fields, namespace listeners, aggregate backend state, and one full bounded `dig` response. Use that evidence before any further retry or architecture change.
- Seventh failure snapshot and root cause: the direct unit was healthy with both listeners, but its aggregate query count remained zero throughout the helper loop. The immediate full diagnostic `dig` then returned the correct answer in `11ms`. The development host uses Bash 3.2: under `set -u`, expanding the declared-but-empty `tcp=()` array fails before `node_shell`, and the loop intentionally hid that stderr while treating the attempt as retryable. Replace the array with explicit UDP and TCP command branches. Keep the semantic/listener gates; no data-plane change is needed.
- Eighth prepare result: explicit UDP/TCP branches fixed the host-side compatibility bug and full prepare passed. Resolver/direct/gateway units are `active/running` at approximately `7.4/6.7/7.0 MiB`, every unit enforces null stdout/stderr, and the resolver runs as the dedicated UID. Both namespaces are present. Policy status remains clean: no resolved drop-in, no vpnctl DNS nftables table, no global DNS/domain override, and original `eth0` DNS/domain/default-route unchanged. This is the accepted prepared boundary for the first full acceptance run.
- First acceptance result: resolved integration and classic capture activated, then the first post-apply gateway-upstream readiness query returned a selected fake IP instead of the authoritative TEST-NET answer. This proves the query came from the ordinary orchestration UID and was correctly redirected by the new classic-port-53 rule; it does not indicate an upstream failure. The EXIT trap completed a full uninstall: units `not-found`, namespaces/user absent, no drop-in/table, and exact original `eth0` DNS/domain/default-route.
- Acceptance correction: authoritative readiness must run as the dedicated resolver UID, the one and only UID excluded from classic capture. Add a separate upstream probe helper using that identity for direct/gateway start and recovery checks. Application probes remain ordinary UID/root so UDP/TCP capture is still exercised.
- Second acceptance result: the dedicated-UID readiness probes passed and the `policy-redir-host` candidate proved strict selected/direct upstream separation through direct Mihomo, the systemd stub, libc/NSS, and `resolvectl`. The run then rejected the `policy-fake-ip` candidate's separation assertion: Mihomo returned a selected synthetic address from `198.19.0.0/16` without querying the gateway authoritative fixture, whose counter contained only the explicit readiness probe. The armed EXIT teardown again completed a full uninstall; all three units are `not-found`, both namespaces and the dedicated user are absent, and the resolved drop-in/nftables table are absent.
- Mode-selection correction: this is provider behavior, not an infrastructure failure. The product contract requires selected-domain queries themselves to use the gateway path and a fresh selected query to fail when gateway DNS is unavailable. Select `policy-redir-host`, which already passed that literal contract, and retain `policy-fake-ip-whitelist` only as a documented rejected candidate: its eager synthetic answer postpones fail-closed behavior until a later connection. Update the remaining capture/cache/outage acceptance to run in `policy-redir-host`; prove the fake candidate's selected-only synthetic behavior and absence of eager gateway lookup without calling it query separation. No retry is permitted until local fixture/contract checks pass.
- Retry preparation result: local shell/Python/JSON/Go contract checks passed and owner-safe prepare again reached the clean prepared boundary with all three no-log units active and both exact namespaces present. The policy remains unapplied. A read-only check observed that `eth0` had meanwhile converged from the earlier transient `198.18.0.2`/`~.` state to networkd's declarative DHCP state: `/run/systemd/network/10-netplan-eth0.network`, `networkctl`, and `resolvectl` agree on DNS `192.168.104.2`, search domains `yandex.net yandex-team.ru yandex.ru lan`, and default-route `yes`. The new pre-prepare baseline records that externally owned state; vpnctl did not write the networkd file, and acceptance must restore and compare this exact current baseline. The earlier observed `198.18.0.2` collision remains valid evidence that a production fake-IP range cannot be hard-coded without a conflict preflight.
- Third acceptance result: redir-host and fake-IP candidate checks completed, followed by classic UDP/TCP capture. The saved nftables evidence records `classic_udp_captured=1`, `classic_tcp_captured=1`, `resolver_uid_bypass=21`, and `resolved_stub_passthrough=123`, so all capture assertions passed. The detached command later exited before cache/outage evidence was written; its stderr was not retained by the executor, so the exact post-capture failing command is not yet attributable. The EXIT trap again fully uninstalled the three units, two namespaces, user, resolved drop-in, and nftables table.
- Third acceptance diagnostic boundary: add a failure-only, sanitized `{stage, exit_code}` evidence file from the already armed EXIT trap before owner-safe teardown. Update the stage only at acceptance boundaries; do not enable service/journal/query logging. Re-run from a clean owner-safe prepare only after local contract checks pass, and use that result instead of speculating about cache or readiness behavior.
- Fourth acceptance result: the new failure artifact identifies `gateway-dns-outage` with exit code `1`; all candidate, classic capture, and direct-cache phases before it passed. The full owner-safe teardown again left the three units `not-found` and both namespaces absent. The coarse stage still covers multiple assertions and does not distinguish a selected leak from cache expiry during the slow `systemctl stop` control round trip.
- Fourth acceptance diagnostic boundary: split gateway outage into sanitized stages for gateway stop, cached-answer retention, fresh selected failure, direct continuation, cache expiry, and recovery. Keep the two-second fixture TTL unchanged until evidence identifies the exact assertion; do not weaken the fail-closed expectation or enable service logs.
- Fifth acceptance result: the granular failure stage is `gateway-dns-outage-cache-expired`. Therefore resolver survival, the within-TTL cached answer, fresh selected failure, and unrelated direct continuation all passed; the only rejected expectation was deletion of a previously accepted answer after its authoritative TTL. Owner-safe teardown is again complete.
- Cache decision correction: pinned Mihomo `v1.19.30` source implements stale-while-revalidate for both cache algorithms: an expired hit is returned with TTL `1`, while an asynchronous gateway refresh is attempted; the default cache capacity is `4096`. This behavior does not query the direct upstream and does not weaken the selected data-plane gateway-or-block guard. Accept it explicitly for a previously gateway-validated answer, while retaining the stronger checks that an unseen selected name fails during gateway-DNS outage and an unseen direct name continues. Acceptance must prove stale TTL `1`, zero selected names in the direct fixture, and a fresh gateway lookup after recovery; it must not claim time-bounded eviction.
- Final acceptance result: `artifacts/v2lab/dns-spike/evidence-20260902T033936Z/summary.json` passed and selects `policy-redir-host`. Selected/direct upstream separation passed through Mihomo, the systemd stub, libc/NSS, and `resolvectl`; the fake-IP whitelist candidate was proven selected-only but rejected because it performed no eager gateway lookup. Classic capture recorded one explicit UDP and one TCP query, 15 resolver-UID bypass packets, and 76 systemd-stub passthrough packets.
- Outage/cache result: an unseen selected name returned no address while gateway DNS was stopped, an unseen direct name still returned its direct answer, and the direct fixture recorded zero selected queries. A previously gateway-validated answer remained available with stale TTL `1`; restarting the gateway fixture restored a fresh selected lookup. Direct cache counts were `1` for the first query, `1` within TTL, and `2` after TTL, proving ordinary expiry/refetch behavior separately from stale-on-error behavior.
- Resource/restoration result: resolver/direct/gateway cgroup memory peaks were `8650752/7700480/7684096` bytes with zero swap. The accepted run restored and byte-compared root nftables, addresses/routes, resolved config hashes, `/etc/resolv.conf` target, link DNS/domains/default-route, and resolved active state both before uninstall and against the pre-prepare baseline. Full uninstall left all three units `not-found`, both namespaces and the dedicated user absent, and no owned drop-in/table/path. Gateway/host and foreign resources were not mutated.

## 2026-09-02 — control PKI, enrollment signature, and JSON RPC spike

### Planned reversible lab mutations

- Decision boundary: task 2.10 selects Ed25519 certificate/signature formats and numeric HTTPS/1.1 JSON RPC limits. It does not install production control state, create an invite/node, open a public enrollment endpoint, alter nginx/transport/routing/firewall, or implement authoritative mutations.
- Exact target: only the contract-matching `vpnctl-v2-gateway` Lima fixture is mutable; the node fixture is a read-only contract check. The foreign `realty-front-docker-vm`, development-host listeners, both lab networks, packages, system services, and all existing vpnctl spike resources are out of scope.
- Owner scope: create only root-owned mode-`0700` `/var/lib/vpnctl-v2-spike-control` with marker `vpnctl-v2-control-spike-v1`, a cross-compiled disposable Go test binary, and its temporary key/certificate/OpenSSL workspace. No private key or transcript is copied back to the development host; ignored evidence contains only test names, tool/resource metadata, hashes, limits, and boolean outcomes.
- Runtime scope: the disposable binary may open only random `127.0.0.1` listeners inside the gateway VM while exercising TLS 1.3, mandatory mTLS, HTTP/1.1, deadlines, request/response/header/JSON-depth sizes, and a 16-connection cap. It writes no service/journal/access log, creates no unit, and exits before evidence is accepted. OpenSSL `3.0.13` is already installed; no package mutation is needed.
- Cryptographic scope: generate ephemeral Ed25519 10-year CAs, five-year server/node leaves with random 128-bit serials, node URI SANs, local node CSRs, a stable enrollment signer, length-prefixed domain-separated transcripts, old/new CA overlap fixtures, and only synthetic public metadata. Every key remains inside the root-only temporary workspace and is removed by teardown.
- Automatic rollback: `verify` arms an EXIT trap immediately after the owner preflight. Success or failure refuses an unowned path or running exact test process, then removes only `/var/lib/vpnctl-v2-spike-control` and a validated `/private/tmp/vpnctl-v2-control-build.*` build directory. Explicit rollback is `./scripts/v2control-spike.sh uninstall`; recreating only the gateway fixture from the versioned template is the last-resort fallback if ownership validation drifts.
- First verify result: the Linux/amd64 test binary built successfully, but `binary_sha=$(build_test_binary)` ran the function in a Bash subshell, so its `host_build_dir` assignment did not propagate and `scp` received `/vpnctl-v2-control-spike.test`. The run stopped before creating the gateway owner path, copying a guest file, or opening a listener; status confirms `state=absent` and `process=absent`. The only residue is exact host temp directory `/private/tmp/vpnctl-v2-control-build.SXuzon` containing the single expected binary with SHA-256 `12d5d08983981036f838f064e8b519feef98b71784c6a40028d8017a0fde8933`.
- First verify correction and rollback: call `build_test_binary` in the parent shell and compute its hash afterward, retaining the path for the armed trap. Remove only the validated temp directory above before retry; it contains no secrets and is fully recoverable by rebuilding from the checked-in test source. Local shell/Go/JSON/contract checks must pass again before VM mutation.
- First executed acceptance result: `artifacts/v2lab/control-spike/evidence-20260902T040016Z/summary.json` passed on Ubuntu/OpenSSL `3.0.13`. Go/OpenSSL Ed25519 CA/CSR/leaf and signature interop, exact URI SAN checks, 180-day renewal boundary, old/new CA chain overlap, transcript context/expiry/replay rejection, TLS 1.3 mTLS, authoritative active/generation checks, malformed/unknown/duplicate/deep/trailing/oversized JSON, response bound, 16-connection cap, and the two-second slow-header deadline passed. The binary completed in `3.33s`, peaked at `13824 KiB` RSS, used zero swap, and full teardown left `state=absent`, `process=absent`, and no host build directory.
- Final acceptance extension: before freezing task 2.10, exercise every selected transport deadline/cap directly rather than relying on configuration inspection. Add real TLS 1.2 rejection, oversized-header rejection, five-second slow-body/read closure, five-second slow-write closure, and five-second idle keep-alive closure. These use only the same random gateway-loopback listeners and owner workspace already journaled; the EXIT/uninstall contract is unchanged.
- Extended acceptance result: `artifacts/v2lab/control-spike/evidence-20260902T040218Z/summary.json` passed and supersedes the first run. Real TLS 1.2, over-limit header, slow header/body/write/idle, and 17th concurrent connection cases were rejected within their selected bounds. The full suite completed in `19.42s`, peaked at `14336 KiB` RSS, used zero swap, and again left `state=absent`, `process=absent`, and no `/private/tmp/vpnctl-v2-control-build.*` directory. Final fixture hardening additionally parses node URI SAN UUIDs as exact canonical hyphen positions plus 16 decoded hexadecimal bytes; this pure validation change requires the standard local/VM gate once more before commit.
- Final acceptance result: `artifacts/v2lab/control-spike/evidence-20260902T040621Z/summary.json` passed the exact committed candidate after canonical UUID parsing. Its summary explicitly records Go/OpenSSL interoperability, URI identity, renewal/CA overlap, transcript context/replay, mTLS and TLS 1.2 rejection, authoritative generation, malformed JSON, header/body/response/depth bounds, all four deadlines, 16-connection cap, and private-loopback-only listener. Peak RSS remained `14336 KiB` with zero swap. Final status is `state=absent`, `process=absent`, and no validated host build directory remains; no package/service/network/firewall/public endpoint or foreign resource was changed.

## 2026-09-02 — encrypted backup KDF and AEAD spike

### Planned reversible lab mutations

- Decision boundary: task 2.11 selects and measures the portable-backup cryptographic envelope only. It does not read production vpnctl state, create a user backup, retain a passphrase, perform restore convergence, install a service/package, or alter routing, firewall, DNS, ingress, transport, swap, or public endpoints.
- Exact mutable target: only the contract-matching `vpnctl-v2-gateway` Lima fixture. The node fixture is a read-only contract/inactivity check. The foreign `realty-front-docker-vm`, development-host services/listeners, and all other VMs/resources remain out of scope.
- Owner scope: create only root-owned mode-`0700` `/var/lib/vpnctl-v2-spike-backup` with marker `vpnctl-v2-backup-spike-v1`, a cross-compiled disposable test binary, and its synthetic temporary archives/restored files. No unit or persistent process is created; all work uses the existing guest `/usr/bin/time` and random local files below the owner root.
- Secret/data scope: the fixed benchmark passphrase has no production value and exists only in disposable process memory; archive fixtures contain deterministic synthetic bytes. Evidence records parameters, timings, byte counts, peak RSS, hashes of the test binary, and boolean outcomes, but no derived key, salt, nonce, ciphertext, plaintext, or passphrase.
- Dependency/build scope: pin official Go module `golang.org/x/crypto` and use only `/private/tmp/vpnctl-v2-backup-{modcache,gocache}` plus a validated `/private/tmp/vpnctl-v2-backup-build.*` directory on the development host. Ordinary build caches under `/private/tmp` are excluded from durable host mutations; the exact build directory is removed automatically.
- Runtime scope: benchmark bounded Argon2id candidates up to `96 MiB`, AES-256-GCM and XChaCha20-Poly1305 chunk candidates over `64 MiB`, then exercise one `17 MiB + 123 byte` streaming archive and bounded corrupt copies. Restore parameters are parsed and capped before KDF allocation. No network socket or external request is made.
- Acceptance: the selected Argon2id profile must remain below `2000 ms` median and `131072 KiB` process RSS on 1 vCPU/512 MiB. The full correctness process, which intentionally performs multiple passphrase/authentication attempts in one CLI lifetime, must remain below `262144 KiB` RSS. Round-trip, mode `0600`, no-overwrite, wrong-passphrase failure, authenticated-header/record/ciphertext corruption, truncation, append rejection, no failed output, and pre-KDF resource limits must all pass.
- Automatic rollback: `verify` arms an EXIT trap after owner preflight and removes only `/var/lib/vpnctl-v2-spike-backup` plus a validated `/private/tmp/vpnctl-v2-backup-build.*` path. Explicit rollback is `./scripts/v2backup-spike.sh uninstall`; recreating only the gateway fixture from the versioned template is the last-resort fallback if ownership validation drifts.
- First verify result: the first Argon2id candidate completed and the later AEAD/correctness suites passed, but summary assembly stopped because `limactl shell` inherited and consumed the remaining candidate rows from the loop's stdin. The partial evidence contains only `m32768-t3-p4` (`560.329 ms` median, `37760 KiB` RSS) and is not acceptance evidence. The EXIT trap removed the exact guest owner path and host build directory; read-only status confirmed `state=absent` and `process=absent`.
- Retry correction: detach only the per-candidate guest command from the loop's control stdin with `/dev/null`, preserving the full five-row candidate stream. Re-run all local checks before the same owner-scoped VM mutation; do not reuse or combine the partial evidence with final acceptance.
- Second verify result: all five KDF and four AEAD candidates plus every correctness case passed, but the summary gate intentionally rejected the original `131072 KiB` whole-suite RSS limit. One selected `64 MiB/t=3/p=4` derivation measured `1103.985 ms` median and `70528 KiB` RSS; the single correctness process peaked at `201056 KiB` because it performs eight sequential derivations for success, wrong-passphrase, and corruption cases and the Go runtime retains reusable heap pages. The fixture remained inside the 512 MiB VM without OOM, and cleanup again confirmed `state=absent` and `process=absent`.
- Resource-bound correction: keep the strict `131072 KiB` limit for one selected KDF operation and use `262144 KiB` for the complete short-lived backup/restore CLI process with repeated authentication attempts. This reflects the accepted three-attempt restore prompt and remains a hard half-host bound; do not force GC or hide the observed allocator behavior. Re-run the unchanged cryptographic cases to produce one self-contained final evidence set.
- Final acceptance result: `artifacts/v2lab/backup-spike/evidence-20260902T053838Z/summary.json` passed on the 1-vCPU/512-MiB fixture. The selected RFC 9106 constrained-memory Argon2id v19 profile (`65536 KiB`, `t=3`, `p=4`, 16-byte salt, 32-byte key) measured `1117.839 ms` median and `70784 KiB` RSS; the five candidates remained below `103424 KiB` RSS. The accepted XChaCha20-Poly1305 1-MiB record profile measured `68.747 MiB/s` combined seal/open throughput, and the 17-MiB streaming archive including KDF encrypted/decrypted in `2384.475/2877.197 ms`.
- Integrity/resource result: mode `0600`, no-overwrite, successful round-trip, wrong-passphrase rejection, authenticated header/index/ciphertext corruption, missing final data, appended data, absent failed output, and pre-KDF parameter caps all passed. The intentionally repeated-failure correctness process peaked at `178528 KiB`, below its `262144 KiB` hard bound. Final cleanup reports `state=absent`, `process=absent`, and no validated host build directory; no package, service, socket, network, firewall, swap, public endpoint, production state, or foreign resource was changed.

## 2026-09-02 — production gateway firewall renderer namespace gate

### Planned reversible lab mutations

- Decision boundary: task 5.4 validates the production `inet vpnctl` gateway firewall renderer and its exact-table replacement batch. It does not apply firewall state to a real gateway, enable a production listener, change sshd, satisfy the watchdog tasks, or operate on Clash Mi/Telegram.
- Exact mutable target: only the contract-matching `vpnctl-v2-node` Lima fixture, used as an isolated Linux kernel test runner. The gateway fixture, development-host network/firewall/listeners, foreign `realty-front-docker-vm`, packages, services, persistent vpnctl paths, and existing lab networks are outside the mutation scope.
- Temporary owner scope: create only `/tmp/vpnctl-v2-firewall-test`, copy the versioned renderer golden plus no-log packet backend/probe helpers, and create exact namespaces `vpnctl-v2-fw-{gateway,overlay,wan,victim}` with six temporary veth ends. No systemd unit, package, user, public listener, host-forwarding rule, or persistent log is created.
- Packet scope: inside the gateway namespace enable only namespaced IPv4/IPv6 forwarding, load a synthetic `inet foreign_keep`, syntax-check/install `inet vpnctl`, and exercise actual TCP/UDP sockets for public allow/drop, closed `443/UDP` and `8443/UDP`, overlay-only internal ports, masqueraded IPv4 Internet egress, private/link-local guard, spoofed-source rejection, all four pool-level lateral directions, and IPv6 fail-closed forwarding/input.
- Preservation/atomicity: byte-capture `foreign_keep`, replace only `inet vpnctl` in one nft batch, byte-compare the foreign table, and rerun one positive plus one negative packet check. The VM root nftables namespace and every foreign root resource remain read-only.
- Automatic rollback: the guest harness arms an `EXIT` trap before namespace creation, terminates only recorded backend PIDs, and deletes only the four exact namespaces. The host wrapper then removes only `/tmp/vpnctl-v2-firewall-test`; explicit rollback is `./scripts/v2firewall-test.sh cleanup`. Recreating only `vpnctl-v2-node` from the pinned template is the last-resort fallback if an exact-name ownership check drifts.
- Preflight result: pinned QEMU/amd64, 1-vCPU, 512-MiB, 10-GiB `vpnctl-v2-node` is running; all four exact namespaces and the runtime root are absent. Local Go race tests, Bash syntax checks, and diff checks pass before mutation.
- Acceptance result: the renderer golden passed `nft --check` and the packet-level JSON gate reported every check true: exact public TCP/UDP allowances, closed `443/UDP` and `8443/UDP`, overlay-only internal ports, loopback and established replies, masqueraded TCP/UDP egress, private-destination and spoofed-source rejection, client/client, client/node, node/client and node/node isolation, IPv6 input/forward fail-closed, byte-identical `foreign_keep`, and successful exact-table atomic replacement.
- Final rollback result: the wrapper and guest `EXIT` traps terminated only their recorded no-log backend PIDs, deleted the four exact namespaces/veths, and removed only `/tmp/vpnctl-v2-firewall-test`. A separate read-only status reports every namespace `absent` and `runtime=absent`; no package, unit, persistent file, public/host listener, root-namespace firewall/route, gateway VM, or foreign resource was changed.
- Harness hardening and final rerun: add a separate reachable `169.254.0.0/16` packet target and require it to block, create the temporary runtime with an exact `vpnctl-v2-firewall-test-v1` owner marker, and make explicit/automatic cleanup refuse an unowned runtime or orphan namespaces. The complete gate passed again with every JSON check true; the final independent status again reports all four namespaces and runtime absent. This supersedes the first successful run as the task 5.4 acceptance result.
- Minimal-init closure: add a second renderer golden with empty internal TCP/UDP sets and no internal allow rules, then run `nft --check` on both minimal-init and populated-service definitions before packet activation. The final complete rerun passed both syntax gates and every packet/preservation check; the independent post-check again reports all four namespaces and owner runtime absent. This is the final task 5.4 evidence.
- Internal-role isolation correction: replace the combined overlay internal-port sets with independent client/node TCP/UDP allowlists selected by source pool. The final packet run proved shared DNS for both roles, node-only control/tunnel access from `10.67.0.0/24`, rejection of the same control/tunnel ports from `10.66.0.0/24`, and all prior public/NAT/lateral/private/link-local/IPv6/preservation checks. Both minimal and populated definitions passed real nft syntax checks; cleanup status remained fully absent. This supersedes the preceding run as the final task 5.4 evidence.
- Public internal-endpoint closure: the exact committed harness additionally runs live `53/TCP` and `17000/TCP` listeners and proves both are blocked from WAN while remaining role-scoped on overlay. The complete gate passed once more and the independent post-check again found every namespace and runtime absent; this is the final accepted run.

## 2026-09-02 — production network watchdog timeout gate

### Planned reversible lab mutations

- Decision boundary: task 5.6 validates the production snapshot/rollback executable and exact 120-second systemd timer. It does not implement `vpnctl confirm`, initialize a gateway/node role, touch a real SSH listener, or apply production networking to either VM root namespace.
- Exact mutable target: only the contract-matching `vpnctl-v2-node` Lima fixture, used as an isolated systemd/kernel runner. The gateway fixture, development-host firewall/routes/listeners, `realty-front-docker-vm`, packages, existing lab networks, and every unrelated guest service/resource are out of scope.
- Namespace scope: create only `/run/netns/vpnctl-v2-watchdog-test`, bring up its loopback device, and create synthetic prior/candidate `inet vpnctl`, reserved route tables `20001/20002`, reserved RPDB priorities `10000/10010/10020`, and three allowlisted namespaced sysctls. A separate `foreign_keep` table, table `21000`, priority `12000`, and unrelated sysctl are byte/JSON controls that must remain unchanged.
- Files/service scope: create owner-marked mode-`0700` `/var/lib/vpnctl`, `/usr/local/libexec/vpnctl-v2-watchdog-test`, and `/tmp/vpnctl-v2-watchdog-test`; install cross-compiled disposable current `vpnctl` and helper binaries; install only `vpnctl-watchdog@.service`, `vpnctl-watchdog@.timer`, and one test-only `NetworkNamespacePath` drop-in. No package, public listener, logging destination, or persistent production state is created.
- Kill/timeout scope: the helper must arm the production 120-second timer, atomically apply a distinguishable candidate, record activation, and receive `SIGKILL`. An unrelated no-log dummy controller process is also killed. The systemd timer must remain active and restore the exact prior vpnctl-owned nftables/routes/RPDB/sysctls no earlier than 120 seconds; all foreign controls must compare unchanged.
- Automatic rollback: `verify` arms an EXIT trap before mutation. Cleanup stops only UUID-shaped watchdog instances, kills only the recorded `/usr/bin/sleep` fixture, deletes only the exact namespace, removes only owner-marked binaries/state/unit templates/drop-in/runtime, reloads systemd, and deletes the validated `/private/tmp/vpnctl-v2-watchdog-build.*` build directory. Explicit rollback is `./scripts/v2watchdog-test.sh cleanup`; recreating only `vpnctl-v2-node` from the pinned template is the last-resort fallback if an ownership marker drifts.
- First verify result: the helper persisted no transaction and applied no candidate because Ubuntu `iproute2` returns exit `2` with `FIB table does not exist` when an unused IPv6 custom table is queried. The EXIT trap removed the exact namespace, units, owner roots, processes, and host build directory; independent status is fully `absent`.
- Retry correction: treat only the stable `FIB table does not exist` result as an empty owned route table during snapshot/flush, while retaining hard failure for every other non-zero route command. Add a unit fixture for this exact Ubuntu behavior, rerun all local gates, then repeat the unchanged isolated 120-second acceptance scenario.
- Second verify result: snapshot, timer start, candidate activation, activation marker, and intentional helper `SIGKILL` completed, but Lima's SSH transport surfaced the remote signal as exit `255` instead of the local-shell `137` expected by the wrapper. The wrapper stopped before waiting for timeout and its EXIT trap disarmed/removed every exact resource; independent status is again fully `absent`.
- Signal-exit correction: accept only `137` or Lima/SSH `255`, then independently require one UUID transaction, its durable `activated.json`, a live timer, and a candidate nftables state different from prior before treating the initiating CLI as killed. These stronger state checks distinguish the expected SSH exit translation from an unrelated connection failure; the 120-second scenario itself remains unchanged.
- Third verify result: the independent timer survived both kills, fired after the full window, wrote the rollback marker, and restored the declared prior nftables/routes/RPDB plus `ip_forward`, `src_valid_mark`, and `rp_filter`. The foreign nftables table, route, and RPDB rule were unchanged. The final foreign-sysctl assertion correctly caught that changing `net.ipv4.ip_forward` itself resets `net.ipv4.conf.all.accept_redirects` from the synthetic host value `0` to the router default `1`; the kernel documents `ip_forward` as a special variable that resets IPv4 configuration parameters. Cleanup again finished fully `absent`.
- Sysctl correction: classify `accept_redirects` as indirectly owned whenever the operation changes `ip_forward`, snapshot it explicitly, and restore `ip_forward` before dependent `net.ipv4.conf.*` values so the kernel reset cannot overwrite their restoration. Move the independent foreign control to `tcp_syncookies` and byte-compare the complete namespaced `net.ipv4.conf.*` surface before/after to detect any additional indirect drift. The exact namespace/unit/file rollback scope is unchanged.
- Fourth verify result: the timer fired, every prior owned and foreign comparison—including the complete `net.ipv4.conf.*` surface—passed after the sysctl ordering correction. Evidence assembly alone failed because the macOS Python `datetime` parser rejected Go's nine-digit RFC3339 nanoseconds. The EXIT trap still completed and independent status is fully `absent`.
- Evidence correction: remove Python from the harness and calculate elapsed time with the checked-in Go helper using `time.RFC3339Nano`, built inside the already validated temporary build directory. Repeat the exact gate so the accepted evidence corresponds to the committed parser and cleanup path.
- Fifth verify result: `artifacts/v2lab/watchdog-test/evidence-20260902T115721Z/summary.json` passed. The systemd timer survived initiating-CLI and dummy-controller `SIGKILL`, fired after `120.094648903` seconds, restored byte/JSON-identical prior `inet vpnctl`, route tables `20001/20002`, reserved RPDB rules, declared sysctls and the complete `net.ipv4.conf.*` surface, and preserved the foreign table/table-21000/priority-12000/`tcp_syncookies` controls. Automatic cleanup and the harness's independent final status reported namespace/state/units/process all `absent`.
- Final transaction-marker hardening: publish one-time markers with atomic no-replace hard links rather than lstat-plus-rename, and reject symlink/non-root-only marker types before any network mutation. This does not widen the lab scope; rerun the exact acceptance gate once more so final evidence matches the committed executable.
- Sixth verify result: the hardened executable again restored all owned state and preserved all foreign controls, but the evidence gate rejected a `117.970458773`-second RFC3339 wall-clock delta. `OnActiveSec` uses systemd's monotonic clock, while the two persisted wall timestamps can move under host/guest time synchronization; cleanup remained fully `absent`.
- Clock-source correction: assert the exact schedule from systemd `ActiveEnterTimestampMonotonic` and `NextElapseUSecMonotonic`, and assert rollback observation against `/proc/uptime` from the same monotonic clock. Retain the RFC3339 delta only as non-gating diagnostic evidence. The production timer remains exactly `OnActiveSec=120s`; no product behavior or mutation scope changes.
- Seventh verify result: systemd 255 exposed an empty/human-formatted next-elapse property for this monotonic template instance, so the property-name gate stopped immediately after arming and cleanup remained fully `absent`. This is an observation API mismatch; the production timer unit still loaded and ran.
- Monotonic evidence correction: the disposable initiating helper now records `CLOCK_MONOTONIC` immediately before its real `systemctl start`, and the same installed helper reads `CLOCK_MONOTONIC` after the rollback marker. Gate the elapsed value at no less than 120 seconds, while the production renderer/unit test remains the exact assertion that the configured schedule is `OnActiveSec=120s`. This avoids wall-clock correction and unstable systemd presentation properties without changing production code.
- Final acceptance result: `artifacts/v2lab/watchdog-test/evidence-20260902T121441Z/summary.json` passed. The independent systemd timer survived both initiating-CLI and dummy-controller `SIGKILL` and was observed after `121.855776494` monotonic seconds for its exact configured 120-second delay. It restored byte/JSON-identical prior `inet vpnctl`, tables `20001/20002`, reserved RPDB rules, declared sysctls and the complete `net.ipv4.conf.*` surface, while preserving the foreign nftables table, table `21000`, priority `12000`, and `tcp_syncookies`. The non-gating wall-clock diagnostic moved independently (`118.317734305` seconds), confirming why monotonic evidence is required. Automatic cleanup and the final independent status left namespace/state/units/process all `absent`.

## 2026-09-02 — new-SSH watchdog confirmation gate

### Planned reversible lab mutations

- Decision boundary: task 5.7 replaces the task-5.6 internal UUID-shaped transaction handle with the already frozen public one-time short form `fw-XXXXXX`, implements `vpnctl confirm <id>`, and proves that endpoint text alone is insufficient: the command must descend from an actual `sshd` process whose established TCP socket matches `SSH_CONNECTION` and whose monotonic start follows activation on the same boot. It does not initialize a production gateway, change sshd, wait through a second timeout, or alter Clash Mi/Telegram resources.
- Exact mutable target: only the contract-matching `vpnctl-v2-node` fixture and the same isolated `/run/netns/vpnctl-v2-watchdog-test` boundary used by task 5.6. The gateway fixture, development-host network/listeners, foreign `realty-front-docker-vm`, packages, SSH configuration/keys, and every unrelated guest unit/resource remain out of scope.
- State/unit scope: temporarily install the current disposable binary/helper plus the two production watchdog templates and test-only namespace drop-in; create only owner-marked `/var/lib/vpnctl`, `/usr/local/libexec/vpnctl-v2-watchdog-test`, `/tmp/vpnctl-v2-watchdog-test`, one minimal valid gateway-role `state.json`, and one short-ID watchdog transaction. The helper applies only the synthetic namespace candidate already used by task 5.6.
- Session cases: the initiating SSH session arms/activates and invokes the public command itself; it must receive validation failure while the timer remains active. A subsequent SSH command must present a different post-activation `sshd`/socket proof, atomically publish `committed.json`, stop the timer, and retain the candidate. A third session reusing the same ID must receive conflict with no mutation. Wrong-port and expired cases remain deterministic unit gates; the real 120-second expiry/rollback path is already covered by the immediately preceding task-5.6 acceptance evidence.
- Preservation and evidence: byte/JSON-capture prior/candidate/committed vpnctl state and the foreign nftables/table-21000/priority-12000/`tcp_syncookies` controls. Accepted evidence may contain the public short transaction ID and redacted result envelopes, but no SSH client address/port, socket inode, state body, or other connection identifier.
- Automatic rollback: the existing EXIT trap stops only the exact short-ID timer/service, deletes only the test namespace and owner-marked files/roots (including `state.json` and all one-time markers), reloads systemd, and removes the validated `/private/tmp/vpnctl-v2-watchdog-build.*` directory. Explicit rollback remains `./scripts/v2watchdog-test.sh cleanup`; recreating only `vpnctl-v2-node` from its pinned template is the last-resort fallback if ownership validation drifts.
- First verify result: the run stopped before arming a transaction or applying the candidate because the harness's ordinary root wrapper intentionally let `sudo` remove `SSH_CONNECTION`; the production helper therefore returned the expected fail-closed `not_ssh` error. The EXIT trap removed the installed files, namespace, state root, and host build directory; independent status is fully `absent`.
- SSH-environment correction: add a separate wrapper used only for the three session-proof invocations that asks `sudo --preserve-env=SSH_CONNECTION` to carry the current Lima/OpenSSH tuple into the root process. Every other guest command retains the ordinary scrubbed environment. This does not trust the text alone: production confirmation must still find its real `sshd` ancestor and an established socket owned by that exact process whose local/remote tuple matches the value. Re-run the same isolated gate after local shell/Go checks.
- Second verify result: before namespace preparation or transaction creation, the first post-install `systemctl daemon-reload` returned a D-Bus timeout on the emulated 1-vCPU guest. The armed trap removed all exact installed paths/units; independent status is fully `absent`. A subsequent read-only check found systemd `running`, no queued jobs, load `0.19`, 303 MiB available RAM, and no persistent failure, so this is a bounded manager-control transient rather than product/session behavior.
- Systemd-control correction: make the lab wrapper retry only `daemon-reload` once after confirming the manager is still `running` or `degraded`; retain immediate failure for any other state or a repeated timeout. This changes neither production units nor acceptance semantics and is used by both install and teardown. Re-run local gates before the same owner-scoped mutation.
- Third verify result: the candidate activated and the initiating-session rejection passed, but the purported new `limactl shell` confirmation was also rejected as `new_ssh_session_required`. Read-only Lima SSH output explains the result: its generated client config uses `ControlMaster=auto`, a persistent `ControlPath`, and `ControlPersist=yes`, so a new local command opens only another channel over the pre-activation TCP/`sshd` process. The production proof correctly rejected that channel. Cleanup again left namespace/state/units/process fully `absent`.
- Fresh-connection correction: retain Lima's generated identity/host-key/port configuration, but for post-activation and replay calls explicitly override only `ControlMaster=no`, `ControlPath=none`, and `ControlPersist=no`. Validate the config path belongs to the exact fixture before use. This forces a distinct TCP handshake and guest `sshd` session without editing SSH configuration, keys, listeners, or the VM; the initiating case continues through the persistent original connection.
- Final acceptance result: `artifacts/v2lab/watchdog-confirm-test/evidence-20260902T153851Z/summary.json` passed with public one-time ID `fw-PZ9GMS`. The initiating session and a second channel over its persistent ControlMaster were rejected; the accepted gate then used a distinct post-activation TCP/`sshd` process whose owned established socket matched the complete `SSH_CONNECTION` tuple. That session atomically published the commit marker, stopped the watchdog timer, and retained the candidate network state. A third fresh session reusing the ID received the frozen conflict result.
- Preservation/rollback result: wrong-port and exact-expiry cases passed the deterministic unit gates, while the preceding real 120-second task-5.6 evidence covers expired rollback timing. Prior/candidate/committed captures proved only the candidate vpnctl resources changed; every foreign nftables/route/RPDB/sysctl control remained identical. The EXIT/explicit cleanup removed the transaction, minimal gateway state, units/drop-in/binaries/runtime, namespace, and validated host build directory; final status reports namespace/state/units/process all `absent`. No SSH config, key, listener, package, real gateway resource, or foreign VM was mutated.

## 2026-09-02 — role-scoped systemd installer isolation gate

### Host-mutation decision

- Task 5.8 was verified without changing either Lima fixture or the development host's systemd/configuration. All filesystem writes used Go test-owned `t.TempDir()` roots, and every `systemctl` call was captured by an in-memory runner rather than executed.
- The gate installs the exact selected-role unit catalog and private generated config, requires each long-running unit to contain exactly `Restart=on-failure`, and rejects foreign-role units, unsafe configs, symlink targets, and start-without-enable before calling systemd.
- Gateway and node cases pre-created foreign-role unit/config sentinels and verified them byte-for-byte after apply. A separate unjoined-node case installed only node files with no `enable` or `start`; its complete manager trace was the synthetic `daemon-reload` call.
- No rollback action is required because no package, service, unit, persistent path, listener, process, network resource, VM, or foreign resource was mutated. Go build and module caches were explicitly redirected to disposable `/tmp/vpnctl-go-{build,mod}` paths.

## 2026-09-02 — idempotent gateway initialization orchestration gate

### Host-mutation decision

- Task 5.9 was implemented and verified without invoking the production composer against the development host or either Lima fixture. All concrete filesystem/systemd installation tests used a unique Go `t.TempDir()` system root; `systemctl`, nftables, sysctl, and watchdog actions were captured by test doubles.
- The read-only plan freezes the explicit public IPv4, default or overridden pools, discovered/overridden external interface, verified SSH listener, fixed `443/TCP`, `8443/TCP`, and `51820/UDP` listeners, clean-host preflight, role unit paths, watchdog templates, editable preset directory, and root-only PKI/enrollment placeholder directories before allocating the host UUID.
- The apply order creates only `/etc/vpnctl`, `/var/lib/vpnctl`, `/run/vpnctl`, their declared children, five gateway process units, and two watchdog templates inside the selected system root. It arms the independent watchdog before installing `inet vpnctl` and changing the four allowlisted IPv4 sysctls; any activation or post-activation error requests immediate rollback of that exact transaction.
- The public `vpnctl init --gateway` grammar now selects this v2 workflow, accepts only the explicit public IP plus the four frozen overrides, and shares the common consent/dry-run/JSON boundary. A successful mutation returns the short watchdog transaction ID and an explicit `vpnctl confirm <id>` action; CLI acceptance used injected temp-root adapters and never invoked production discovery or mutation.
- Gateway component units have their final role-scoped names and `Restart=on-failure`, but remain protected by component-specific readiness-file conditions until their controller/DNS/transport/tunnel implementations are delivered by later tasks. No temporary keys or preset schema were invented: PKI directories and empty valid state collections are the task-5.9 placeholders, while cryptographic material and built-in YAML sources remain assigned to tasks 6.2 and 7.1–7.2.
- Acceptance used the concrete temp-root layout, state, role-unit, and watchdog-unit installers plus in-memory network/watchdog adapters. It proved a byte-valid generation-1 gateway state, exact modes, absence of node units, and zero filesystem/systemd/network/watchdog mutation on a second identical init. No cleanup or recovery is required because no real package, service, unit, persistent path, listener, process, firewall, route, sysctl, VM, or foreign resource was changed.

## 2026-09-02 — idempotent unjoined-node initialization gate

### Host-mutation decision

- Task 5.10 was implemented and verified without invoking the production composer against the development host or either Lima fixture. Every concrete filesystem/systemd check used a unique Go `t.TempDir()` system root, and the only `systemctl` trace was captured by an in-memory runner.
- The public `vpnctl init --node` path accepts no role-specific arguments and uses the common confirmation, `--dry-run`, `--yes`, and JSON workflow. An existing gateway role is rejected before discovery or construction of mutating adapters; a repeated node init validates the stored role and host ID and performs no filesystem or systemd action.
- A fresh apply creates only node-owned `/etc/vpnctl`, `/var/lib/vpnctl`, `/run/vpnctl` roots and the minimum root-only secrets/snapshots/operations children. It deliberately does not create gateway preset, export, backup, PKI, enrollment, or watchdog trees and preserves unrelated application paths byte-for-byte.
- Initialization installs only `vpnctl-routing.service`, `vpnctl-standard.service`, and `vpnctl-tunnel-client.service` plus the private node bootstrap config. All three long-running units use `Restart=on-failure`, are guarded by node readiness files, and remain disabled and stopped until a later successful `join` writes the enrolled identity and generated data-plane configuration. Gateway controller, DNS, restricted listener, tunnel-server, ingress, and watchdog units are absent.
- The generation-1 node state contains the immutable host role/ID and component manifest with explicit empty arrays. It contains no local enrolled node identity, gateway trust, policy, transport, expose, certificate, credential file, or active tunnel. Acceptance proves the concrete manager trace is exactly synthetic `daemon-reload`, the secrets directory is empty, and a second identical init has no effect.
- No cleanup or recovery is required because no real package, service, unit, persistent path, listener, process, tunnel, route, DNS setting, firewall rule, sysctl, VM, or foreign resource was changed. Go build/module artifacts were confined to disposable `/tmp` paths: `/tmp/vpnctl-go-cache`, `/tmp/vpnctl-go-test-cache`, `/tmp/vpnctl-go-mod-cache`, `/tmp/vpnctl-node-init-modcache`, and `/tmp/vpnctl-node-init-{gocache,test-cache,race-cache,vet-cache,help-cache}`.

## 2026-09-02 — optional managed-swap lifecycle gate

### Host-mutation decision

- Task 5.11 was implemented and verified without invoking production swap commands or systemd against the development host or either Lima fixture. Concrete file/unit tests used only unique Go `t.TempDir()` system roots; `fallocate`, `mkswap`, `swapon`, `swapoff`, and every `systemctl` operation were captured and statefully emulated by an in-memory runner.
- Gateway init now emits a read-only swap disposition. It offers one fixed 1-GiB resource only below 1 GiB RAM, with less than 1 GiB existing aggregate swap, and at least 1.5 GiB free disk so 512 MiB remains after allocation. Unknown resource capacity and insufficient disk make only this optional action unavailable; an otherwise valid init can continue without swap. Existing adequate swap remains foreign and untouched.
- Interactive init asks a separate yes/no swap question before ordinary init consent; answering no continues initialization, while `--yes` explicitly accepts both ordinary confirmations. Dry-run reports the offer without selecting or creating it. Only an accepted creation adds the fixed `/var/lib/vpnctl/swapfile`, 1-GiB size, mode `0600`, enabled ownership record to generation-1 state.
- Persistence uses the exact owned `vpnctl-managed-swap.service` and does not edit `/etc/fstab`. Creation uses exclusive file creation inside the root-only state directory, exact-size allocation, `mkswap`, atomic unit installation, daemon reload, enable, and start. Partial creation cleans up its exact file/unit; a later initial-state persistence failure also purges the just-created swap before returning.
- Status compares the authoritative record with exact file type/mode/size, unit content, enabled state, and active state. Lifecycle teardown refuses non-standard records, altered units, and symlinks. Uninstall semantics stop/disable/remove only the unit and preserve the allocation file for recoverability with state; purge additionally deletes only the exact regular swap file. Tests byte-preserve foreign targets and broader state directories.
- No cleanup or recovery is required because no real swap, `/etc/fstab`, package, unit, service, persistent path, listener, process, network resource, sysctl, VM, or foreign resource was changed. Go build/module artifacts remained in disposable `/tmp/vpnctl-node-init-modcache` and `/tmp/vpnctl-swap-gocache` only.

## 2026-09-02 — gateway controller process and local control gate

### Host-mutation decision

- Task 6.1 was implemented without installing or starting the production controller, changing systemd, or touching `/etc/vpnctl`, `/var/lib/vpnctl`, or `/run/vpnctl` on the development host or either Lima fixture. Concrete state, lock, and socket checks used unique `/tmp/vpnctl-controller-test-*` roots owned and removed by each Go test.
- The controller is now the single gateway-authoritative writer in one process and across processes: it holds the root-only `/run/vpnctl/state.lock`, serves bounded strict JSON on `/run/vpnctl/control.sock` inside the mode-`0700` runtime directory, and sets both lock and socket to `0600`. A second controller cannot remove the live socket; after lock ownership is acquired, only a stale socket may be replaced.
- Accepted mutations are serialized through one critical section covering authoritative reload, dispatch, generation-checked atomic state save, and post-commit observation. Twelve simultaneous local requests produced twelve sequential generations with maximum dispatcher concurrency one. Request-ID replay and mandatory expected-generation semantics remain explicitly assigned to task 6.5.
- Startup and local observation use only bounded `systemctl show` calls for role-owned units. There is no reconcile loop and no start, stop, restart, reload, enable, disable, pending apply, or drift repair path in the observer. Observation failures become bounded status issues and do not mutate or prevent controller admission.
- Graceful termination closes the listener to new work, waits for already accepted requests to commit, removes only the exact socket inode it created, and releases the writer lock. Two controller lifecycles around an active data-plane mock left its start/stop/restart counters at zero and preserved the authoritative generation; the controller systemd unit has a 30-second stop bound and preserves the shared runtime directory across restarts.
- Gateway role installation now writes the exact private `gateway-controller.ready` marker, so `vpnctl-controller.service` starts after generation-1 gateway state exists. Other gateway data-plane services retain their absent readiness markers and remain condition-guarded until their implementation tasks. The internal `vpnctl __service gateway-controller` entrypoint is signal-bound, hidden from public help, and emits no ordinary output.
- The local, full, race, vet, dependency-direction, and strict OpenSpec gates passed. Socket tests required only permission to bind local temporary sockets because the command sandbox blocks bind; they created no public listener. All sockets and test roots were removed automatically. Remaining build/module caches are disposable `/tmp/vpnctl-controller-*-gocache` and `/tmp/vpnctl-node-init-modcache` directories.
- Source rollback is the single task-6.1 commit. No runtime rollback or cleanup is required because no real package, service, unit, persistent vpnctl path, network rule, sysctl, VM, or foreign resource was mutated.

## 2026-09-02 — control PKI and enrollment identity gate

### Host-mutation decision

- Task 6.2 was implemented and verified without initializing vpnctl on the development host or either Lima fixture. Every certificate, private key, state file, and directory created by acceptance tests lived under a unique Go `t.TempDir()` system root and was removed automatically; no production path, package, service, listener, network resource, VM, or foreign resource changed.
- A fresh gateway initialization now creates an Ed25519 self-signed control CA, an independently keyed Ed25519 gateway server leaf, and a third independent Ed25519 enrollment signing identity. The gateway leaf carries only the canonical gateway URI and derived internal-overlay IPv4 SAN. The control CA and five-year leaf metadata are committed with the generation-1 authoritative state only after all six secret artifacts are durably published.
- Node CSR primitives generate the Ed25519 private key locally in PKCS#8 PEM and request exactly one canonical node URI SAN. Issuance verifies the CSR signature and authoritative node ID, rejects alternate key algorithms and any additional SAN, ignores the mutable CSR common name, and constructs a five-year client leaf under the control CA. Positive certificate serials consume independent entropy and remain bounded to 128 bits.
- Root-only provisioning uses mode-`0700` secret directories and mode-`0600` files with no-replace atomic publication. A concurrent or pre-existing identity conflict preserves the existing bytes and rolls back only exact references created by the failed attempt. Failed initialization before state persistence removes the new identity; failures after the state commit retain the now-authoritative identity for recovery.
- The internal control CA, gateway leaf, and enrollment signer have separate keys, fingerprints, metadata, and secret references. No `public-ingress` certificate or RSA key is created by this task: the public RSA/IP identity remains an explicitly separate trust domain and implementation task 12.1.
- Full unit/integration, race-detector, vet, package-list/dependency-direction, strict OpenSpec, and formatting gates passed. Source rollback is the single task-6.2 commit; no runtime cleanup is required because the tests left no persistent host or fixture mutation. Disposable Go caches remain only under `/tmp/vpnctl-pki-*-gocache` and the pre-existing disposable module-cache path `/tmp/vpnctl-node-init-modcache`.

## 2026-09-02 — internal mTLS control RPC transport gate

### Host-mutation decision

- Task 6.3 was implemented and verified without starting the production gateway controller, applying the new firewall artifact, or changing either Lima fixture. Network tests bound only ephemeral `127.0.0.1` TCP ports inside the test process; all listeners, certificates, connections, and `t.TempDir()` material were closed or removed automatically. No public listener, package, service, persistent path, network rule, VM, or foreign resource changed.
- The control server derives its sole accepted bind identity from the configured node CIDR and refuses wildcard, public, or alternate listener addresses. Production `ListenAndServe` uses only the derived gateway overlay IPv4 on fixed `9443/TCP`; gateway init now admits that port only from the node pool on `vpnctl-wg`, while WAN and personal-client sources remain denied.
- Server and short-lived node client require TLS 1.3, Ed25519 certificates under the internal control CA, exact gateway/node URI identities, the gateway overlay IP SAN, and HTTP/1.1 ALPN. Every client call constructs and closes a one-request transport with redirects, compression, keep-alive, and HTTP/2 disabled. A CA-valid certificate with an extra SAN, a certificate/envelope node mismatch, no client certificate, and TLS 1.2 are rejected.
- The wire boundary uses one POST under `/rpc/v1/<operation>` with strict duplicate/unknown/trailing-field rejection, object-only typed payloads, canonical nonce/UUID shapes, certificate-bound node identity, and typed bounded responses. Limits remain the accepted 64-KiB request, 256-KiB response, 8-KiB header, depth 32, 2-second header, 5-second body/write/idle, 120-second timestamp skew, and 16 simultaneously accepted connections.
- Acceptance covered success over mTLS, separate connections for repeated CLI calls, wildcard-bind refusal, malformed/duplicate/unknown/deep/trailing JSON, wrong path/operation/content type, request/response overflow, oversized headers, slow header/body/handler paths, and rejection of a 17th session. Protocol-major compatibility, idempotent commit/replay, and authoritative active-node/credential-generation checks remain isolated to tasks 6.4, 6.5, and 6.6 respectively.
- Full unit/integration, race-detector, vet, package-list/dependency-direction, strict OpenSpec, and formatting gates passed. Source rollback is the single task-6.3 commit; no runtime cleanup is required. Disposable Go caches remain only under `/tmp/vpnctl-rpc-*-gocache` and the pre-existing disposable module-cache path `/tmp/vpnctl-node-init-modcache`.

## 2026-09-02 — control protocol compatibility and dispatch gate

### Host-mutation decision

- Task 6.4 changed only protocol/model code, deterministic fixtures, OpenSpec state, and this journal. Its integration tests reused the task-6.3 ephemeral loopback mTLS fixture and closed every connection/listener automatically. No production controller, service, persistent path, firewall, route, VM, public endpoint, or foreign resource was changed, so no runtime rollback is required.
- Protocol identity is now canonical `major.minor` and independent from `vpnctl_version`. The ordered release-manifest window contains one current maximum minor plus at most one immediately previous major maximum minor; leading-zero forms, duplicate/same-major windows, non-adjacent majors, reversed order, and more than two registrations are rejected.
- HTTPS paths are versioned as `/rpc/v<major>/<operation>` and must match the envelope major. A registry dispatches current and previous majors to distinct compiled handlers. Within one major, every requested minor from zero through that handler's declared maximum is treated as an additive-compatible request and the exact negotiated version is echoed in the typed response.
- A client minor newer than the gateway maximum, an unknown breaking major, a missing compiled handler, or a path/envelope major mismatch returns a deterministic `incompatible_protocol` or `protocol_path_mismatch` conflict before any operation handler runs. The response lists only the two bounded supported maxima and an explicit update action.
- The gateway-first fixture exposed current `2.2` and previous `1.4` simultaneously over mTLS: node `1.3` reached only the previous handler, node `2.1` reached only current, while `2.3`, `3.0`, and mismatched paths were rejected without dispatch. This is generic compatibility evidence; the v2.0 production manifest remains independently pinned to initial control protocol `1.0`.
- Full unit/integration, race-detector, vet, package-list/dependency-direction, strict OpenSpec, and formatting gates passed. Source rollback is the single task-6.4 commit. Disposable caches remain only below `/tmp/vpnctl-protocol-*-gocache` and `/tmp/vpnctl-node-init-modcache`.

## 2026-09-02 — effectively-once control mutation gate

### Host-mutation decision

- Task 6.5 changed only controller code, deterministic test-owned state, OpenSpec progress, and this journal. Tests used unique `/tmp/vpnctl-controller-test-*` roots and the existing ephemeral loopback/Unix-socket fixtures; they closed or removed every listener, state root, and temporary artifact automatically. No production controller, service, persistent vpnctl path, firewall, route, VM, public endpoint, or foreign resource was changed, so no runtime rollback is required.
- A node mutation handler now shares the controller's sole mutation lock with local mutations. The serialized critical section covers authoritative reload, retained-result lookup, expected-generation decision, resource dispatch, normalized result hashing, bounded per-node idempotency append, one generation-checked atomic state save, and post-commit passive observation.
- Remote mutations require a positive `expected_state_generation`. A retained matching `request_id` returns the stored redacted summary, original result hash, and original resulting generation before the stale-generation check; it never calls the resource dispatcher. Reusing an ID for another operation conflicts. Only status/hash/generation/operation metadata enters authoritative state; RPC payloads, response bodies, secrets, and sensitive paths are not persisted.
- A non-retained request at the current generation may execute once. A future generation conflicts immediately. A stale request can invoke only the resource-specific read-only reconciliation path: a proven current outcome returns a determined result at the authoritative generation, while an unprovable outcome conflicts. Neither case blindly dispatches or writes state, which safely covers both an evicted uncertain retry and an unknown stale request.
- Response-loss acceptance discarded the first committed response and retried the exact ID/generation: the effect and dispatcher each ran once, generation advanced once, and replay returned the retained hash/generation. Twenty-four simultaneous copies of one request also produced one effect and one record. Twelve distinct concurrent requests at one generation produced exactly one commit plus eleven reconciliation conflicts, with no partial state. A time-travel retry just beyond 30 days reconciled the existing effect without replay or generation change; an unprovable retry conflicted.
- Full unit/integration and race-detector suites, vet, package listing/dependency direction, strict OpenSpec validation, formatting, and diff checks passed. Source rollback is the single task-6.5 commit. Disposable Go caches remain only below `/tmp/vpnctl-mutation-*-gocache` and `/tmp/vpnctl-node-init-modcache`.
